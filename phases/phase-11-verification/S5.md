# S5 · Ф11 — Проверка

**понедельник 23 ноября — четверг 26 ноября**

Ваша зона: мобильное приложение, QA, DevOps.

| | |
| --- | --- |
| Задач у вас в этой фазе | **3** |
| Значит коммитов | **3** — по одному на задачу |
| Ваш PR смотрит и вливает | **S1 — капитан** |

---

## Шаг 0 · воскресенье 22 ноября — завести свои задачи в Jira

На разборе фазы. **До того, как написана первая строчка кода.**

Создайте 3 задачи в колонке `To Do`. Название копируйте дословно:

- **`BF-94 Run the phase specifications and record results`** · тип `Story` · эпик `BF-E12` · исполнитель — вы
- **`BF-95 Correct the specifications after the run`** · тип `Story` · эпик `BF-E12` · исполнитель — вы
- **`BF-96 Add the Postman collection`** · тип `Story` · эпик `BF-E12` · исполнитель — вы

Пока задачи не заведены у всех пятерых, фаза не начинается.

---

## Задача 1 из 3 · понедельник 23 ноября

### `BF-94 Run the phase specifications and record results`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-11 && git pull
git checkout -b phase-11/<ваше-имя>-BF-94
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-11/aslan-BF-94`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| изменить | `1.md` | — |
| изменить | `6.md` | — |
| создать | `docs/verification/**` | скриншоты |
| создать | `api/internal/api/criteria_test.go` | — |
| создать | `api/internal/api/phase10_test.go` | — |
| создать | `api/internal/api/phase12_test.go` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A 1.md 6.md api/internal/api/criteria_test.go \
        api/internal/api/phase10_test.go api/internal/api/phase12_test.go docs/verification/**
git commit -m "BF-94 Run the phase specifications and record results"
git push -u origin phase-11/<ваше-имя>-BF-94
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-11` ← ветка фазы, **не** `main`
- **compare:** `phase-11/<ваше-имя>-BF-94`
- **заголовок:** `BF-94 Run the phase specifications and record results`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 2 из 3 · вторник 24 ноября

### `BF-95 Correct the specifications after the run`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-11 && git pull
git checkout -b phase-11/<ваше-имя>-BF-95
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-11/aslan-BF-95`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `web/playwright.config.ts` | — |
| создать | `web/e2e/fixtures.ts` | — |
| создать | `web/e2e/auth.spec.ts` | — |
| создать | `web/e2e/free-event.spec.ts` | — |
| создать | `web/e2e/paid-checkout.spec.ts` | — |
| создать | `web/e2e/analytics-and-gate.spec.ts` | — |
| создать | `web/e2e/support-and-moderation.spec.ts` | — |
| изменить | `10.md` | — |
| изменить | `2.md` | — |
| изменить | `4.md` | — |
| изменить | `7.md` | — |
| изменить | `8.md` | — |
| изменить | `9.md` | спецификации правятся под фактическое поведение |
| изменить | `.github/workflows/ci.yml` | — |
| изменить | `web/Dockerfile` | — |
| изменить | `web/.dockerignore` | — |
| изменить | `api/Dockerfile` | — |
| изменить | `api/.dockerignore` | e2e требует собранных образов |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A .github/workflows/ci.yml 10.md 2.md \
        4.md 7.md 8.md \
        9.md api/.dockerignore api/Dockerfile \
        web/.dockerignore web/Dockerfile web/e2e/analytics-and-gate.spec.ts \
        web/e2e/auth.spec.ts web/e2e/fixtures.ts web/e2e/free-event.spec.ts \
        web/e2e/paid-checkout.spec.ts web/e2e/support-and-moderation.spec.ts web/playwright.config.ts
git commit -m "BF-95 Correct the specifications after the run"
git push -u origin phase-11/<ваше-имя>-BF-95
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-11` ← ветка фазы, **не** `main`
- **compare:** `phase-11/<ваше-имя>-BF-95`
- **заголовок:** `BF-95 Correct the specifications after the run`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 3 из 3 · среда 25 ноября

### `BF-96 Add the Postman collection`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-11 && git pull
git checkout -b phase-11/<ваше-имя>-BF-96
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-11/aslan-BF-96`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `docs/biletflow-api.postman_collection.json` | 2 177 строк, целиком |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверять сборку нечего** — в этом коммите только текст.

**5. Коммит и пуш:**

```bash
git add -A docs/biletflow-api.postman_collection.json
git commit -m "BF-96 Add the Postman collection"
git push -u origin phase-11/<ваше-имя>-BF-96
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-11` ← ветка фазы, **не** `main`
- **compare:** `phase-11/<ваше-имя>-BF-96`
- **заголовок:** `BF-96 Add the Postman collection`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## среда 25 ноября — ревью

Своего кода больше не пишем. Если капитан вернул PR с замечаниями — правите и пишете в PR «готово». Если замечаний нет — свободны.

## четверг 26 ноября — закрытие фазы

Приходите на разбор. Убедитесь, что ваши задачи в `Done` — их переводит капитан после слияния. Сразу после разбора заводите свои задачи следующей фазы.

---

## Кто смотрит ваш PR

Все PR в команде смотрит и вливает **капитан (S1)**. Он проверяет, что файлы только из вашей зоны, что коммит один и назван как задача, и что CI зелёный. Он же вливает PR и переводит задачу в `Done`.

Друг друга не ревьюим: одно место принятия решений — меньше путаницы и не бывает «а я думал, ты посмотришь».

---

## Если что-то пошло не так

| Ситуация | Что делать |
| --- | --- |
| CI покраснел | Чините у себя, делаете ещё коммит в ту же ветку, пушите. PR обновится сам. |
| Капитан вернул с замечаниями | Правите, коммитите в ту же ветку, пишете в PR «готово». Новый PR не нужен. |
| Забыли файл в коммите | Добавляете вторым коммитом в ту же ветку. Правило «одна задача — один коммит» про то, что приходит один смысловой шаг, а не про запрет себя исправлять. |
| Не успеваете к концу фазы | Говорите на разборе. Задача переезжает в следующую фазу **со своим номером**, новую взамен не заводят. |
| Нужен чужой файл | Пишете владельцу. Сами не правите никогда. |

