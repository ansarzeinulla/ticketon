# S1 · Ф8 — Возвраты, поддержка, админ

**понедельник 2 ноября — воскресенье 8 ноября**

Ваша зона: бэкенд-ядро, база данных, интеграция.

| | |
| --- | --- |
| Задач у вас в этой фазе | **3** |
| Значит коммитов | **3** — по одному на задачу |
| Ваш PR смотрит и вливает | **вы сами: вы капитан** |

---

## Шаг 0 · воскресенье 1 ноября — завести свои задачи в Jira

На разборе фазы. **До того, как написана первая строчка кода.**

Создайте 3 задачи в колонке `To Do`. Название копируйте дословно:

- **`BF-60 Add refund, support and report tables`** · тип `Story` · эпик `BF-E8` · исполнитель — вы
- **`BF-61 Add the report queue and settings`** · тип `Story` · эпик `BF-E10` · исполнитель — вы
- **`BF-62 Give every conflict its own error code`** · тип `Story` · эпик `BF-E1` · исполнитель — вы

Пока задачи не заведены у всех пятерых, фаза не начинается.

---

## Задача 1 из 3 · понедельник 2 ноября

### `BF-60 Add refund, support and report tables`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-08 && git pull
git checkout -b phase-08/<ваше-имя>-BF-60
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-08/aslan-BF-60`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| изменить | `db/init/02_schema.sql` | +5 таблиц |
| изменить | `api/internal/api/server.go` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make test          # тесты базы
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/server.go db/init/02_schema.sql
git commit -m "BF-60 Add refund, support and report tables"
git push -u origin phase-08/<ваше-имя>-BF-60
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-08` ← ветка фазы, **не** `main`
- **compare:** `phase-08/<ваше-имя>-BF-60`
- **заголовок:** `BF-60 Add refund, support and report tables`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Вы капитан, поэтому свой PR вливаете сами — но только когда CI зелёный. Порядок ревью и слияния — в `captain.md`.

---

## Задача 2 из 3 · четверг 5 ноября

### `BF-61 Add the report queue and settings`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-08 && git pull
git checkout -b phase-08/<ваше-имя>-BF-61
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-08/aslan-BF-61`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `api/internal/store/admin.go` | — |
| создать | `api/internal/api/handlers_admin_portal.go` | — |
| создать | `api/internal/api/handlers_admin_users.go` | — |
| создать | `api/internal/store/moderation.go` | — |
| создать | `api/internal/api/handlers_moderation.go` | — |
| создать | `api/internal/api/handlers_admin.go` | — |
| создать | `api/internal/store/reports.go` | отдельный стор очереди жалоб |
| создать | `api/internal/api/portal_test.go` | — |
| создать | `api/internal/api/moderation_test.go` | — |
| создать | `api/internal/api/admin_users_test.go` | — |
| создать | `db/tests/06_end_to_end_flow.sql` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make test          # тесты базы
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/admin_users_test.go api/internal/api/handlers_admin.go api/internal/api/handlers_admin_portal.go \
        api/internal/api/handlers_admin_users.go api/internal/api/handlers_moderation.go api/internal/api/moderation_test.go \
        api/internal/api/portal_test.go api/internal/store/admin.go api/internal/store/moderation.go \
        api/internal/store/reports.go db/tests/06_end_to_end_flow.sql
git commit -m "BF-61 Add the report queue and settings"
git push -u origin phase-08/<ваше-имя>-BF-61
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-08` ← ветка фазы, **не** `main`
- **compare:** `phase-08/<ваше-имя>-BF-61`
- **заголовок:** `BF-61 Add the report queue and settings`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Вы капитан, поэтому свой PR вливаете сами — но только когда CI зелёный. Порядок ревью и слияния — в `captain.md`.

---

## Задача 3 из 3 · пятница 6 ноября

### `BF-62 Give every conflict its own error code`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-08 && git pull
git checkout -b phase-08/<ваше-имя>-BF-62
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-08/aslan-BF-62`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| удалить | `db/tests/07_temp_checks.sql` | вытеснен `05_business_rules.sql` |
| изменить | `api/internal/store/audit.go` | — |
| изменить | `api/internal/store/notifications.go` | — |
| изменить | `api/internal/store/profiles.go` | — |
| изменить | `api/internal/store/store.go` | — |
| изменить | `api/internal/store/store_test.go` | поиск по всем сущностям потребовал новых выборок |
| изменить | `db/tests/01_environment.sql` | — |
| изменить | `db/tests/02_schema_objects.sql` | — |
| изменить | `db/tests/03_user_crud.sql` | — |
| изменить | `db/tests/04_constraints.sql` | — |
| изменить | `db/tests/05_business_rules.sql` | — |
| изменить | `db/tests/_fixture.sql` | — |
| изменить | `db/tests/_helpers.sql` | — |
| изменить | `db/tests/run_tests.sh` | общий `conflict` разошёлся на именованные коды |
| изменить | `api/internal/testutil/testdb.go` | наборы тестов разрослись, фикстура вынесена |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make test          # тесты базы
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/store/audit.go api/internal/store/notifications.go api/internal/store/profiles.go \
        api/internal/store/store.go api/internal/store/store_test.go api/internal/testutil/testdb.go \
        db/tests/01_environment.sql db/tests/02_schema_objects.sql db/tests/03_user_crud.sql \
        db/tests/04_constraints.sql db/tests/05_business_rules.sql db/tests/07_temp_checks.sql \
        db/tests/_fixture.sql db/tests/_helpers.sql db/tests/run_tests.sh
git commit -m "BF-62 Give every conflict its own error code"
git push -u origin phase-08/<ваше-имя>-BF-62
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-08` ← ветка фазы, **не** `main`
- **compare:** `phase-08/<ваше-имя>-BF-62`
- **заголовок:** `BF-62 Give every conflict its own error code`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Вы капитан, поэтому свой PR вливаете сами — но только когда CI зелёный. Порядок ревью и слияния — в `captain.md`.

---

## суббота 7 ноября — ревью

Своего кода больше не пишем. Вы капитан: ваша работа в этот день — прочитать и влить все оставшиеся PR. Порядок — в `captain.md`.

## воскресенье 8 ноября — закрытие фазы

Приходите на разбор. Убедитесь, что ваши задачи в `Done` — их переводит капитан после слияния. Сразу после разбора заводите свои задачи следующей фазы.

---

## Кто смотрит ваш PR

Все PR смотрит и вливает **капитан** — то есть вы. Что проверять и как вливать, расписано в `captain.md` этой же папки.

---

## Если что-то пошло не так

| Ситуация | Что делать |
| --- | --- |
| CI покраснел | Чините у себя, делаете ещё коммит в ту же ветку, пушите. PR обновится сам. |
| Капитан вернул с замечаниями | Правите, коммитите в ту же ветку, пишете в PR «готово». Новый PR не нужен. |
| Забыли файл в коммите | Добавляете вторым коммитом в ту же ветку. Правило «одна задача — один коммит» про то, что приходит один смысловой шаг, а не про запрет себя исправлять. |
| Не успеваете к концу фазы | Говорите на разборе. Задача переезжает в следующую фазу **со своим номером**, новую взамен не заводят. |
| Нужен чужой файл | Пишете владельцу. Сами не правите никогда. |

