# S4 · Ф8 — Возвраты, поддержка, админ

**понедельник 2 ноября — воскресенье 8 ноября**

Ваша зона: веб организатора и админа, аналитика, поддержка.

| | |
| --- | --- |
| Задач у вас в этой фазе | **4** |
| Значит коммитов | **4** — по одному на задачу |
| Ваш PR смотрит и вливает | **S1 — капитан** |

---

## Шаг 0 · воскресенье 1 ноября — завести свои задачи в Jira

На разборе фазы. **До того, как написана первая строчка кода.**

Создайте 4 задачи в колонке `To Do`. Название копируйте дословно:

- **`BF-67 Open a support case with context`** · тип `Story` · эпик `BF-E8` · исполнитель — вы
- **`BF-68 Add the admin portal`** · тип `Story` · эпик `BF-E10` · исполнитель — вы
- **`BF-B2 Move the session into an httpOnly cookie`** · тип `Bug` · — · исполнитель — вы
- **`BF-B3 Rename middleware to proxy for Next 16`** · тип `Bug` · — · исполнитель — вы

Пока задачи не заведены у всех пятерых, фаза не начинается.

---

## Задача 1 из 4 · вторник 3 ноября

### `BF-67 Open a support case with context`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-08 && git pull
git checkout -b phase-08/<ваше-имя>-BF-67
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-08/aslan-BF-67`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `api/internal/store/support.go` | — |
| создать | `api/internal/api/handlers_support.go` | — |
| создать | `web/src/components/support-inbox.tsx` | — |
| создать | `web/src/components/support-thread.tsx` | — |
| создать | `api/internal/api/support_test.go` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/handlers_support.go api/internal/api/support_test.go api/internal/store/support.go \
        web/src/components/support-inbox.tsx web/src/components/support-thread.tsx
git commit -m "BF-67 Open a support case with context"
git push -u origin phase-08/<ваше-имя>-BF-67
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-08` ← ветка фазы, **не** `main`
- **compare:** `phase-08/<ваше-имя>-BF-67`
- **заголовок:** `BF-67 Open a support case with context`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 2 из 4 · среда 4 ноября

### `BF-68 Add the admin portal`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-08 && git pull
git checkout -b phase-08/<ваше-имя>-BF-68
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-08/aslan-BF-68`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| переименовать | `api/internal/store/guests.go` → `api/internal/store/attendees.go` | в SRS термин «attendee», а не «guest» |
| создать | `web/src/app/(app)/admin/page.tsx` | — |
| создать | `web/src/app/(app)/admin/admin-portal.tsx` | — |
| создать | `web/src/components/moderation-action.tsx` | — |
| создать | `web/src/app/(app)/admin/reports/page.tsx` | — |
| создать | `web/src/app/(app)/admin/reports/reports-queue.tsx` | — |
| изменить | `api/internal/store/attendees.go` | поиск по всем сущностям потребовал новых выборок |
| изменить | `api/internal/api/handlers_attendees.go` | общий `conflict` разошёлся на именованные коды |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git mv api/internal/store/guests.go api/internal/store/attendees.go
git add -A api/internal/api/handlers_attendees.go api/internal/store/attendees.go web/src/app/(app)/admin/admin-portal.tsx \
        web/src/app/(app)/admin/page.tsx web/src/app/(app)/admin/reports/page.tsx web/src/app/(app)/admin/reports/reports-queue.tsx \
        web/src/components/moderation-action.tsx
git commit -m "BF-68 Add the admin portal"
git push -u origin phase-08/<ваше-имя>-BF-68
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-08` ← ветка фазы, **не** `main`
- **compare:** `phase-08/<ваше-имя>-BF-68`
- **заголовок:** `BF-68 Add the admin portal`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 3 из 4 · четверг 5 ноября

### `BF-B2 Move the session into an httpOnly cookie`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-08 && git pull
git checkout -b phase-08/<ваше-имя>-BF-B2
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-08/aslan-BF-B2`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| изменить | `web/src/lib/session.ts` | `-38/+14`, три функции **удалены** |
| создать | `web/src/lib/server-session.ts` | — |
| создать | `web/src/app/api/auth/login/route.ts` | — |
| создать | `web/src/app/api/auth/logout/route.ts` | — |
| создать | `web/src/app/api/auth/me/route.ts` | — |
| создать | `web/src/app/api/auth/register/route.ts` | — |
| создать | `web/src/app/api/proxy/[...path]/route.ts` | — |
| изменить | `web/src/lib/auth-context.tsx` | — |
| удалить | `web/src/app/api/health/route.ts` | вытеснен прокси-маршрутом |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A web/src/app/api/auth/login/route.ts web/src/app/api/auth/logout/route.ts web/src/app/api/auth/me/route.ts \
        web/src/app/api/auth/register/route.ts web/src/app/api/health/route.ts web/src/app/api/proxy/[...path]/route.ts \
        web/src/lib/auth-context.tsx web/src/lib/server-session.ts web/src/lib/session.ts
git commit -m "BF-B2 Move the session into an httpOnly cookie"
git push -u origin phase-08/<ваше-имя>-BF-B2
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-08` ← ветка фазы, **не** `main`
- **compare:** `phase-08/<ваше-имя>-BF-B2`
- **заголовок:** `BF-B2 Move the session into an httpOnly cookie`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 4 из 4 · пятница 6 ноября

### `BF-B3 Rename middleware to proxy for Next 16`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-08 && git pull
git checkout -b phase-08/<ваше-имя>-BF-B3
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-08/aslan-BF-B3`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| переименовать | `web/src/middleware.ts` → `web/src/proxy.ts` | — |
| изменить | `web/src/proxy.ts` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git mv web/src/middleware.ts web/src/proxy.ts
git add -A web/src/proxy.ts
git commit -m "BF-B3 Rename middleware to proxy for Next 16"
git push -u origin phase-08/<ваше-имя>-BF-B3
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-08` ← ветка фазы, **не** `main`
- **compare:** `phase-08/<ваше-имя>-BF-B3`
- **заголовок:** `BF-B3 Rename middleware to proxy for Next 16`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## суббота 7 ноября — ревью

Своего кода больше не пишем. Если капитан вернул PR с замечаниями — правите и пишете в PR «готово». Если замечаний нет — свободны.

## воскресенье 8 ноября — закрытие фазы

Приходите на разбор. Убедитесь, что ваши задачи в `Done` — их переводит капитан после слияния. Сразу после разбора заводите свои задачи следующей фазы.

---

## Кто смотрит ваш PR

Все PR в команде смотрит и вливает **капитан (S1)**. Он проверяет, что файлы только из вашей зоны, что коммит один и назван как задача, и что CI зелёный. Он же вливает PR и переводит задачу в `Done`.

Друг друга не ревьюим: одно место принятия решений — меньше путаницы и не бывает «а я думал, ты посмотришь».

---

## Если что-то пошло не так

| Ситуация | Что делать |
| --- | --- |
| CI покраснел | Чините у себя, делаете ещё коммит в ту же ветку, пушите. PR обновится сам. |
| Капитан вернул с замечаниями | Правите, коммитите в ту же ветку, пишете в PR «готово». Новый PR не нужен. |
| Забыли файл в коммите | Добавляете вторым коммитом в ту же ветку. Правило «одна задача — один коммит» про то, что приходит один смысловой шаг, а не про запрет себя исправлять. |
| Не успеваете к концу фазы | Говорите на разборе. Задача переезжает в следующую фазу **со своим номером**, новую взамен не заводят. |
| Нужен чужой файл | Пишете владельцу. Сами не правите никогда. |

