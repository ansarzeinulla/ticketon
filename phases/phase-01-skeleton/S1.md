# S1 · Ф1 — Скелет

**понедельник 14 сентября — воскресенье 20 сентября**

Ваша зона: бэкенд-ядро, база данных, интеграция.

| | |
| --- | --- |
| Задач у вас в этой фазе | **3** |
| Значит коммитов | **3** — по одному на задачу |
| Ваш PR смотрит и вливает | **вы сами: вы капитан** |

---

## Шаг 0 · воскресенье 13 сентября — завести свои задачи в Jira

На разборе фазы. **До того, как написана первая строчка кода.**

Создайте 3 задачи в колонке `To Do`. Название копируйте дословно:

- **`BF-6 Add extensions and the users table`** · тип `Story` · эпик `BF-E1` · исполнитель — вы
- **`BF-8 Add the pool and a health route`** · тип `Story` · эпик `BF-E1` · исполнитель — вы
- **`BF-7 Add config and the error envelope`** · тип `Story` · эпик `BF-E1` · исполнитель — вы

Пока задачи не заведены у всех пятерых, фаза не начинается.

---

## Задача 1 из 3 · понедельник 14 сентября

### `BF-6 Add extensions and the users table`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-01 && git pull
git checkout -b phase-01/<ваше-имя>-BF-6
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-01/aslan-BF-6`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `db/init/01_extensions.sql` | — |
| создать | `db/init/02_schema.sql` | 1 таблица, ~40 строк |
| создать | `db/init/03_sample.sql` | пара строк, чтобы было что выбрать |
| создать | `db/tests/run_tests.sh` | — |
| создать | `db/tests/_helpers.sql` | — |
| создать | `db/tests/_fixture.sql` | — |
| создать | `db/tests/01_environment.sql` | — |
| создать | `db/tests/02_tables.sql` | — |
| создать | `db/tests/00_smoke.sql` | проверка «база вообще поднялась» |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make test          # тесты базы
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A db/init/01_extensions.sql db/init/02_schema.sql db/init/03_sample.sql \
        db/tests/00_smoke.sql db/tests/01_environment.sql db/tests/02_tables.sql \
        db/tests/_fixture.sql db/tests/_helpers.sql db/tests/run_tests.sh
git commit -m "BF-6 Add extensions and the users table"
git push -u origin phase-01/<ваше-имя>-BF-6
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-01` ← ветка фазы, **не** `main`
- **compare:** `phase-01/<ваше-имя>-BF-6`
- **заголовок:** `BF-6 Add extensions and the users table`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Вы капитан, поэтому свой PR вливаете сами — но только когда CI зелёный. Порядок ревью и слияния — в `captain.md`.

---

## Задача 2 из 3 · среда 16 сентября

### `BF-8 Add the pool and a health route`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-01 && git pull
git checkout -b phase-01/<ваше-имя>-BF-8
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-01/aslan-BF-8`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `api/internal/database/database.go` | — |
| создать | `api/internal/store/store.go` | — |
| создать | `api/internal/api/server.go` | 1 маршрут, ~40 строк |
| создать | `api/internal/api/handlers_dev.go` | дев-роут с дампом конфига |
| создать | `api/internal/api/handlers_health.go` | одна ручка /health отдельным файлом |
| создать | `api/internal/config/config_test.go` | — |
| изменить | `api/internal/config/config.go` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/handlers_dev.go api/internal/api/handlers_health.go api/internal/api/server.go \
        api/internal/config/config.go api/internal/config/config_test.go api/internal/database/database.go \
        api/internal/store/store.go
git commit -m "BF-8 Add the pool and a health route"
git push -u origin phase-01/<ваше-имя>-BF-8
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-01` ← ветка фазы, **не** `main`
- **compare:** `phase-01/<ваше-имя>-BF-8`
- **заголовок:** `BF-8 Add the pool and a health route`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Вы капитан, поэтому свой PR вливаете сами — но только когда CI зелёный. Порядок ревью и слияния — в `captain.md`.

---

## Задача 3 из 3 · четверг 17 сентября

### `BF-7 Add config and the error envelope`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-01 && git pull
git checkout -b phase-01/<ваше-имя>-BF-7
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-01/aslan-BF-7`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `api/go.mod` | — |
| создать | `api/go.sum` | — |
| создать | `api/cmd/api/main.go` | — |
| создать | `api/internal/config/config.go` | — |
| создать | `api/internal/httpx/httpx.go` | — |
| создать | `api/internal/config/env.go` | чтение переменных окружения врозь |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/cmd/api/main.go api/go.mod api/go.sum \
        api/internal/config/config.go api/internal/config/env.go api/internal/httpx/httpx.go
git commit -m "BF-7 Add config and the error envelope"
git push -u origin phase-01/<ваше-имя>-BF-7
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-01` ← ветка фазы, **не** `main`
- **compare:** `phase-01/<ваше-имя>-BF-7`
- **заголовок:** `BF-7 Add config and the error envelope`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Вы капитан, поэтому свой PR вливаете сами — но только когда CI зелёный. Порядок ревью и слияния — в `captain.md`.

---

## суббота 19 сентября — ревью

Своего кода больше не пишем. Вы капитан: ваша работа в этот день — прочитать и влить все оставшиеся PR. Порядок — в `captain.md`.

## воскресенье 20 сентября — закрытие фазы

Приходите на разбор. Убедитесь, что ваши задачи в `Done` — их переводит капитан после слияния. Сразу после разбора заводите свои задачи следующей фазы.

---

## Кто смотрит ваш PR

Все PR смотрит и вливает **капитан** — то есть вы. Что проверять и как вливать, расписано в `captain.md` этой же папки.

---

## Если что-то пошло не так

| Ситуация | Что делать |
| --- | --- |
| CI покраснел | Чините у себя, делаете ещё коммит в ту же ветку, пушите. PR обновится сам. |
| Капитан вернул с замечаниями | Правите, коммитите в ту же ветку, пишете в PR «готово». Новый PR не нужен. |
| Забыли файл в коммите | Добавляете вторым коммитом в ту же ветку. Правило «одна задача — один коммит» про то, что приходит один смысловой шаг, а не про запрет себя исправлять. |
| Не успеваете к концу фазы | Говорите на разборе. Задача переезжает в следующую фазу **со своим номером**, новую взамен не заводят. |
| Нужен чужой файл | Пишете владельцу. Сами не правите никогда. |

