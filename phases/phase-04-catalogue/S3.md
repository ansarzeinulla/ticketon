# S3 · Ф4 — Каталог и бесплатная регистрация

**понедельник 5 октября — воскресенье 11 октября**

Ваша зона: веб для зрителя, типы и клиент API, локализация.

| | |
| --- | --- |
| Задач у вас в этой фазе | **3** |
| Значит коммитов | **3** — по одному на задачу |
| Ваш PR смотрит и вливает | **S1 — капитан** |

---

## Шаг 0 · воскресенье 4 октября — завести свои задачи в Jira

На разборе фазы. **До того, как написана первая строчка кода.**

Создайте 3 задачи в колонке `To Do`. Название копируйте дословно:

- **`BF-32 Add the public catalogue`** · тип `Story` · эпик `BF-E11` · исполнитель — вы
- **`BF-33 Add the order page`** · тип `Story` · эпик `BF-E11` · исполнитель — вы
- **`BF-34 Add the formatting dependencies`** · тип `Story` · эпик `BF-E1` · исполнитель — вы

Пока задачи не заведены у всех пятерых, фаза не начинается.

---

## Задача 1 из 3 · среда 7 октября

### `BF-32 Add the public catalogue`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-04 && git pull
git checkout -b phase-04/<ваше-имя>-BF-32
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-04/aslan-BF-32`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `web/src/app/events/page.tsx` | — |
| создать | `web/src/components/event-card.tsx` | — |
| изменить | `web/src/lib/api.ts` | — |
| изменить | `web/src/lib/types.ts` | — |
| удалить | `web/src/lib/mock-data.ts` | вытеснен реальными вызовами `api.ts` |
| создать | `web/src/app/events/[slug]/page.tsx` | — |
| создать | `web/src/app/events/layout.tsx` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A web/src/app/events/[slug]/page.tsx web/src/app/events/layout.tsx web/src/app/events/page.tsx \
        web/src/components/event-card.tsx web/src/lib/api.ts web/src/lib/mock-data.ts \
        web/src/lib/types.ts
git commit -m "BF-32 Add the public catalogue"
git push -u origin phase-04/<ваше-имя>-BF-32
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-04` ← ветка фазы, **не** `main`
- **compare:** `phase-04/<ваше-имя>-BF-32`
- **заголовок:** `BF-32 Add the public catalogue`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 2 из 3 · четверг 8 октября

### `BF-33 Add the order page`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-04 && git pull
git checkout -b phase-04/<ваше-имя>-BF-33
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-04/aslan-BF-33`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `web/src/components/checkout-dialog.tsx` | — |
| создать | `web/src/lib/money.ts` | — |
| создать | `web/src/lib/money.test.ts` | — |
| создать | `web/src/app/orders/[id]/page.tsx` | — |
| изменить | `web/src/lib/api.ts` | — |
| изменить | `web/src/lib/types.ts` | — |
| создать | `web/src/components/order-list.tsx` | первый список заказов |
| создать | `web/src/app/events/[slug]/ticket-selector.tsx` | — |
| создать | `web/src/lib/datetime.ts` | — |
| создать | `web/src/lib/datetime.test.ts` | — |
| изменить | `web/src/app/events/[slug]/page.tsx` | `-52/+9` |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A web/src/app/events/[slug]/page.tsx web/src/app/events/[slug]/ticket-selector.tsx web/src/app/orders/[id]/page.tsx \
        web/src/components/checkout-dialog.tsx web/src/components/order-list.tsx web/src/lib/api.ts \
        web/src/lib/datetime.test.ts web/src/lib/datetime.ts web/src/lib/money.test.ts \
        web/src/lib/money.ts web/src/lib/types.ts
git commit -m "BF-33 Add the order page"
git push -u origin phase-04/<ваше-имя>-BF-33
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-04` ← ветка фазы, **не** `main`
- **compare:** `phase-04/<ваше-имя>-BF-33`
- **заголовок:** `BF-33 Add the order page`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 3 из 3 · пятница 9 октября

### `BF-34 Add the formatting dependencies`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-04 && git pull
git checkout -b phase-04/<ваше-имя>-BF-34
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-04/aslan-BF-34`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| удалить | `web/src/lib/env.ts` | вытеснен переменными `NEXT_PUBLIC_*` |
| удалить | `web/src/lib/format.ts` | вытеснен `money.ts` и `datetime.ts` |
| изменить | `web/package.json` | — |
| изменить | `web/package-lock.json` | — |
| изменить | `web/tsconfig.json` | деньги и даты на витрине |
| изменить | `web/.gitignore` | появился `.next` |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A web/.gitignore web/package-lock.json web/package.json \
        web/src/lib/env.ts web/src/lib/format.ts web/tsconfig.json
git commit -m "BF-34 Add the formatting dependencies"
git push -u origin phase-04/<ваше-имя>-BF-34
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-04` ← ветка фазы, **не** `main`
- **compare:** `phase-04/<ваше-имя>-BF-34`
- **заголовок:** `BF-34 Add the formatting dependencies`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## суббота 10 октября — ревью

Своего кода больше не пишем. Если капитан вернул PR с замечаниями — правите и пишете в PR «готово». Если замечаний нет — свободны.

## воскресенье 11 октября — закрытие фазы

Приходите на разбор. Убедитесь, что ваши задачи в `Done` — их переводит капитан после слияния. Сразу после разбора заводите свои задачи следующей фазы.

---

## Кто смотрит ваш PR

Все PR в команде смотрит и вливает **капитан (S1)**. Он проверяет, что файлы только из вашей зоны, что коммит один и назван как задача, и что CI зелёный. Он же вливает PR и переводит задачу в `Done`.

Друг друга не ревьюим: одно место принятия решений — меньше путаницы и не бывает «а я думал, ты посмотришь».

---

## Если что-то пошло не так

| Ситуация | Что делать |
| --- | --- |
| CI покраснел | Чините у себя, делаете ещё коммит в ту же ветку, пушите. PR обновится сам. |
| Капитан вернул с замечаниями | Правите, коммитите в ту же ветку, пишете в PR «готово». Новый PR не нужен. |
| Забыли файл в коммите | Добавляете вторым коммитом в ту же ветку. Правило «одна задача — один коммит» про то, что приходит один смысловой шаг, а не про запрет себя исправлять. |
| Не успеваете к концу фазы | Говорите на разборе. Задача переезжает в следующую фазу **со своим номером**, новую взамен не заводят. |
| Нужен чужой файл | Пишете владельцу. Сами не правите никогда. |

