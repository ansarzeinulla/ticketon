# S1 · Ф2 — Идентичность

**понедельник 21 сентября — воскресенье 27 сентября**

Ваша зона: бэкенд-ядро, база данных, интеграция.

| | |
| --- | --- |
| Задач у вас в этой фазе | **3** |
| Значит коммитов | **3** — по одному на задачу |
| Ваш PR смотрит и вливает | **вы сами: вы капитан** |

---

## Шаг 0 · воскресенье 20 сентября — завести свои задачи в Jira

На разборе фазы. **До того, как написана первая строчка кода.**

Создайте 3 задачи в колонке `To Do`. Название копируйте дословно:

- **`BF-14 Hash passwords with bcrypt`** · тип `Story` · эпик `BF-E2` · исполнитель — вы
- **`BF-15 Reject duplicate emails with 409`** · тип `Story` · эпик `BF-E1` · исполнитель — вы
- **`BF-16 Cover users, tokens and mail`** · тип `Story` · эпик `BF-E1` · исполнитель — вы

Пока задачи не заведены у всех пятерых, фаза не начинается.

---

## Задача 1 из 3 · понедельник 21 сентября

### `BF-14 Hash passwords with bcrypt`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-02 && git pull
git checkout -b phase-02/<ваше-имя>-BF-14
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-02/aslan-BF-14`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `api/internal/auth/password.go` | — |
| создать | `api/internal/auth/password_test.go` | — |
| создать | `api/internal/auth/token.go` | — |
| создать | `api/internal/auth/token_test.go` | — |
| создать | `api/internal/httpx/context.go` | — |
| создать | `api/internal/api/middleware.go` | — |
| изменить | `db/init/02_schema.sql` | — |
| создать | `api/internal/store/users.go` | 2 функции |
| удалить | `api/internal/config/env.go` | слито в `config.go` |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make test          # тесты базы
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/middleware.go api/internal/auth/password.go api/internal/auth/password_test.go \
        api/internal/auth/token.go api/internal/auth/token_test.go api/internal/config/env.go \
        api/internal/httpx/context.go api/internal/store/users.go db/init/02_schema.sql
git commit -m "BF-14 Hash passwords with bcrypt"
git push -u origin phase-02/<ваше-имя>-BF-14
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-02` ← ветка фазы, **не** `main`
- **compare:** `phase-02/<ваше-имя>-BF-14`
- **заголовок:** `BF-14 Hash passwords with bcrypt`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Вы капитан, поэтому свой PR вливаете сами — но только когда CI зелёный. Порядок ревью и слияния — в `captain.md`.

---

## Задача 2 из 3 · вторник 22 сентября

### `BF-15 Reject duplicate emails with 409`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-02 && git pull
git checkout -b phase-02/<ваше-имя>-BF-15
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-02/aslan-BF-15`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `api/internal/api/handlers_auth.go` | — |
| создать | `api/internal/api/validate.go` | — |
| изменить | `api/internal/api/server.go` | — |
| удалить | `api/internal/api/handlers_health.go` | ручка переехала в `server.go` |
| изменить | `api/internal/api/handlers_auth.go` | — |
| изменить | `api/internal/store/users.go` | — |
| создать | `api/internal/api/main_test.go` | — |
| создать | `api/internal/api/auth_test.go` | — |
| создать | `api/internal/api/fixtures_test.go` | фикстуры тестов лежали отдельно |
| изменить | `db/init/02_schema.sql` | — |
| создать | `api/internal/store/tokens.go` | — |
| создать | `api/internal/api/handlers_account.go` | — |
| создать | `api/internal/email/email.go` | — |
| создать | `api/internal/email/templates.go` | — |
| создать | `api/internal/email/console.go` | печать письма в консоль |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make test          # тесты базы
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/auth_test.go api/internal/api/fixtures_test.go api/internal/api/handlers_account.go \
        api/internal/api/handlers_auth.go api/internal/api/handlers_health.go api/internal/api/main_test.go \
        api/internal/api/server.go api/internal/api/validate.go api/internal/email/console.go \
        api/internal/email/email.go api/internal/email/templates.go api/internal/store/tokens.go \
        api/internal/store/users.go db/init/02_schema.sql
git commit -m "BF-15 Reject duplicate emails with 409"
git push -u origin phase-02/<ваше-имя>-BF-15
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-02` ← ветка фазы, **не** `main`
- **compare:** `phase-02/<ваше-имя>-BF-15`
- **заголовок:** `BF-15 Reject duplicate emails with 409`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Вы капитан, поэтому свой PR вливаете сами — но только когда CI зелёный. Порядок ревью и слияния — в `captain.md`.

---

## Задача 3 из 3 · пятница 25 сентября

### `BF-16 Cover users, tokens and mail`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-02 && git pull
git checkout -b phase-02/<ваше-имя>-BF-16
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-02/aslan-BF-16`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| переименовать | `db/tests/02_tables.sql` → `db/tests/02_schema_objects.sql` | — |
| изменить | `api/internal/store/tokens.go` | — |
| создать | `api/internal/api/account_test.go` | — |
| создать | `db/tests/03_user_crud.sql` | — |
| создать | `db/tests/04_constraints.sql` | — |
| создать | `api/internal/store/store_test.go` | — |
| создать | `api/internal/email/email_test.go` | — |
| удалить | `db/tests/00_smoke.sql` | вытеснен полноценным набором |
| изменить | `db/tests/02_schema_objects.sql` | набор проверяет уже не только таблицы, но и enum'ы со связями |
| изменить | `api/internal/httpx/httpx.go` | — |
| изменить | `api/internal/database/database.go` | конверт ошибок и пул подросли под аутентификацию |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make test          # тесты базы
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git mv db/tests/02_tables.sql db/tests/02_schema_objects.sql
git add -A api/internal/api/account_test.go api/internal/database/database.go api/internal/email/email_test.go \
        api/internal/httpx/httpx.go api/internal/store/store_test.go api/internal/store/tokens.go \
        db/tests/00_smoke.sql db/tests/02_schema_objects.sql db/tests/03_user_crud.sql \
        db/tests/04_constraints.sql
git commit -m "BF-16 Cover users, tokens and mail"
git push -u origin phase-02/<ваше-имя>-BF-16
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-02` ← ветка фазы, **не** `main`
- **compare:** `phase-02/<ваше-имя>-BF-16`
- **заголовок:** `BF-16 Cover users, tokens and mail`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Вы капитан, поэтому свой PR вливаете сами — но только когда CI зелёный. Порядок ревью и слияния — в `captain.md`.

---

## суббота 26 сентября — ревью

Своего кода больше не пишем. Вы капитан: ваша работа в этот день — прочитать и влить все оставшиеся PR. Порядок — в `captain.md`.

## воскресенье 27 сентября — закрытие фазы

Приходите на разбор. Убедитесь, что ваши задачи в `Done` — их переводит капитан после слияния. Сразу после разбора заводите свои задачи следующей фазы.

---

## Кто смотрит ваш PR

Все PR смотрит и вливает **капитан** — то есть вы. Что проверять и как вливать, расписано в `captain.md` этой же папки.

---

## Если что-то пошло не так

| Ситуация | Что делать |
| --- | --- |
| CI покраснел | Чините у себя, делаете ещё коммит в ту же ветку, пушите. PR обновится сам. |
| Капитан вернул с замечаниями | Правите, коммитите в ту же ветку, пишете в PR «готово». Новый PR не нужен. |
| Забыли файл в коммите | Добавляете вторым коммитом в ту же ветку. Правило «одна задача — один коммит» про то, что приходит один смысловой шаг, а не про запрет себя исправлять. |
| Не успеваете к концу фазы | Говорите на разборе. Задача переезжает в следующую фазу **со своим номером**, новую взамен не заводят. |
| Нужен чужой файл | Пишете владельцу. Сами не правите никогда. |

