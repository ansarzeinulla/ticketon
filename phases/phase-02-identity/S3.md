# S3 · Ф2 — Идентичность

**понедельник 21 сентября — воскресенье 27 сентября**

Ваша зона: веб для зрителя, типы и клиент API, локализация.

| | |
| --- | --- |
| Задач у вас в этой фазе | **2** |
| Значит коммитов | **2** — по одному на задачу |
| Ваш PR смотрит и вливает | **S1 — капитан** |

---

## Шаг 0 · воскресенье 20 сентября — завести свои задачи в Jira

На разборе фазы. **До того, как написана первая строчка кода.**

Создайте 2 задачи в колонке `To Do`. Название копируйте дословно:

- **`BF-18 Add the API client`** · тип `Story` · эпик `BF-E1` · исполнитель — вы
- **`BF-19 Add reset and verify pages`** · тип `Story` · эпик `BF-E2` · исполнитель — вы

Пока задачи не заведены у всех пятерых, фаза не начинается.

---

## Задача 1 из 2 · среда 23 сентября

### `BF-18 Add the API client`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-02 && git pull
git checkout -b phase-02/<ваше-имя>-BF-18
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-02/aslan-BF-18`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `web/src/lib/api.ts` | ~80 строк, прямой `:8080` |
| создать | `web/src/lib/types.ts` | — |
| создать | `web/src/lib/format.ts` | самодельные форматтеры денег и дат |
| создать | `web/src/lib/env.ts` | адрес API читался вручную |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A web/src/lib/api.ts web/src/lib/env.ts web/src/lib/format.ts \
        web/src/lib/types.ts
git commit -m "BF-18 Add the API client"
git push -u origin phase-02/<ваше-имя>-BF-18
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-02` ← ветка фазы, **не** `main`
- **compare:** `phase-02/<ваше-имя>-BF-18`
- **заголовок:** `BF-18 Add the API client`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 2 из 2 · четверг 24 сентября

### `BF-19 Add reset and verify pages`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-02 && git pull
git checkout -b phase-02/<ваше-имя>-BF-19
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-02/aslan-BF-19`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `web/src/app/(auth)/layout.tsx` | — |
| создать | `web/src/app/(auth)/login/page.tsx` | — |
| создать | `web/src/app/(auth)/register/page.tsx` | — |
| изменить | `web/src/lib/api.ts` | — |
| создать | `web/src/app/(auth)/forgot-password/page.tsx` | — |
| создать | `web/src/app/(auth)/reset-password/page.tsx` | — |
| создать | `web/src/app/(auth)/verify-email/page.tsx` | — |
| изменить | `web/src/lib/types.ts` | — |
| создать | `web/src/app/(auth)/login/login-form.tsx` | — |
| создать | `web/src/app/(auth)/register/register-form.tsx` | — |
| создать | `web/src/app/(auth)/forgot-password/forgot-password-form.tsx` | — |
| создать | `web/src/app/(auth)/reset-password/reset-password-form.tsx` | — |
| создать | `web/src/app/(auth)/verify-email/verify-email-view.tsx` | — |
| изменить | `web/package.json` | — |
| изменить | `web/package-lock.json` | формы входа потребовали валидации |
| изменить | `web/README.md` | появились страницы входа |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A web/README.md web/package-lock.json web/package.json \
        web/src/app/(auth)/forgot-password/forgot-password-form.tsx web/src/app/(auth)/forgot-password/page.tsx web/src/app/(auth)/layout.tsx \
        web/src/app/(auth)/login/login-form.tsx web/src/app/(auth)/login/page.tsx web/src/app/(auth)/register/page.tsx \
        web/src/app/(auth)/register/register-form.tsx web/src/app/(auth)/reset-password/page.tsx web/src/app/(auth)/reset-password/reset-password-form.tsx \
        web/src/app/(auth)/verify-email/page.tsx web/src/app/(auth)/verify-email/verify-email-view.tsx web/src/lib/api.ts \
        web/src/lib/types.ts
git commit -m "BF-19 Add reset and verify pages"
git push -u origin phase-02/<ваше-имя>-BF-19
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-02` ← ветка фазы, **не** `main`
- **compare:** `phase-02/<ваше-имя>-BF-19`
- **заголовок:** `BF-19 Add reset and verify pages`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## суббота 26 сентября — ревью

Своего кода больше не пишем. Если капитан вернул PR с замечаниями — правите и пишете в PR «готово». Если замечаний нет — свободны.

## воскресенье 27 сентября — закрытие фазы

Приходите на разбор. Убедитесь, что ваши задачи в `Done` — их переводит капитан после слияния. Сразу после разбора заводите свои задачи следующей фазы.

---

## Кто смотрит ваш PR

Все PR в команде смотрит и вливает **капитан (S1)**. Он проверяет, что файлы только из вашей зоны, что коммит один и назван как задача, и что CI зелёный. Он же вливает PR и переводит задачу в `Done`.

Друг друга не ревьюим: одно место принятия решений — меньше путаницы и не бывает «а я думал, ты посмотришь».

---

## Если что-то пошло не так

| Ситуация | Что делать |
| --- | --- |
| CI покраснел | Чините у себя, делаете ещё коммит в ту же ветку, пушите. PR обновится сам. |
| Капитан вернул с замечаниями | Правите, коммитите в ту же ветку, пишете в PR «готово». Новый PR не нужен. |
| Забыли файл в коммите | Добавляете вторым коммитом в ту же ветку. Правило «одна задача — один коммит» про то, что приходит один смысловой шаг, а не про запрет себя исправлять. |
| Не успеваете к концу фазы | Говорите на разборе. Задача переезжает в следующую фазу **со своим номером**, новую взамен не заводят. |
| Нужен чужой файл | Пишете владельцу. Сами не правите никогда. |

