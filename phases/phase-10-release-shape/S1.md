# S1 · Ф10 — Нынешняя версия

**понедельник 16 ноября — воскресенье 22 ноября**

Ваша зона: бэкенд-ядро, база данных, интеграция.

| | |
| --- | --- |
| Задач у вас в этой фазе | **4** |
| Значит коммитов | **4** — по одному на задачу |
| Ваш PR смотрит и вливает | **вы сами: вы капитан** |

---

## Шаг 0 · воскресенье 15 ноября — завести свои задачи в Jira

На разборе фазы. **До того, как написана первая строчка кода.**

Создайте 4 задачи в колонке `To Do`. Название копируйте дословно:

- **`BF-B6 Make the seed re-runnable after a purchase`** · тип `Bug` · — · исполнитель — вы
- **`BF-80 Count text limits in characters, not bytes`** · тип `Story` · эпик `BF-E1` · исполнитель — вы
- **`BF-81 Count text limits in the remaining handlers`** · тип `Story` · эпик `BF-E1` · исполнитель — вы
- **`BF-B5 Stop sharing notification state on the server`** · тип `Bug` · — · исполнитель — вы

Пока задачи не заведены у всех пятерых, фаза не начинается.

---

## Задача 1 из 4 · вторник 17 ноября

### `BF-B6 Make the seed re-runnable after a purchase`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-10 && git pull
git checkout -b phase-10/<ваше-имя>-BF-B6
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-10/aslan-BF-B6`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| изменить | `db/seed/01_demo_data.sql` | `-12/+40`, порядок FK и upsert |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make test          # тесты базы
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A db/seed/01_demo_data.sql
git commit -m "BF-B6 Make the seed re-runnable after a purchase"
git push -u origin phase-10/<ваше-имя>-BF-B6
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-10` ← ветка фазы, **не** `main`
- **compare:** `phase-10/<ваше-имя>-BF-B6`
- **заголовок:** `BF-B6 Make the seed re-runnable after a purchase`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Вы капитан, поэтому свой PR вливаете сами — но только когда CI зелёный. Порядок ревью и слияния — в `captain.md`.

---

## Задача 2 из 4 · среда 18 ноября

### `BF-80 Count text limits in characters, not bytes`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-10 && git pull
git checkout -b phase-10/<ваше-имя>-BF-80
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-10/aslan-BF-80`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| изменить | `api/internal/api/validate.go` | `-25/+90` |
| изменить | `api/internal/api/handlers_auth.go` | — |
| изменить | `api/internal/api/handlers_account.go` | — |
| изменить | `api/internal/api/handlers_profile.go` | — |
| изменить | `api/internal/auth/password.go` | `-18/+30`, `ErrPasswordTooLong` **удалён** |
| изменить | `api/internal/auth/password_test.go` | тест **удалён** |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/handlers_account.go api/internal/api/handlers_auth.go api/internal/api/handlers_profile.go \
        api/internal/api/validate.go api/internal/auth/password.go api/internal/auth/password_test.go
git commit -m "BF-80 Count text limits in characters, not bytes"
git push -u origin phase-10/<ваше-имя>-BF-80
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-10` ← ветка фазы, **не** `main`
- **compare:** `phase-10/<ваше-имя>-BF-80`
- **заголовок:** `BF-80 Count text limits in characters, not bytes`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Вы капитан, поэтому свой PR вливаете сами — но только когда CI зелёный. Порядок ревью и слияния — в `captain.md`.

---

## Задача 3 из 4 · четверг 19 ноября

### `BF-81 Count text limits in the remaining handlers`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-10 && git pull
git checkout -b phase-10/<ваше-имя>-BF-81
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-10/aslan-BF-81`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| изменить | `api/internal/api/server.go` | — |
| изменить | `api/internal/api/admin_users_test.go` | — |
| изменить | `api/internal/api/moderation_test.go` | — |
| изменить | `api/internal/api/portal_test.go` | тесты считали байты вслед за кодом |
| удалить | `api/internal/store/reports.go` | слит в `admin.go` |
| изменить | `api/internal/store/admin.go` | — |
| изменить | `api/internal/store/moderation.go` | общий `conflict` дочищен до именованных кодов |
| изменить | `api/internal/api/handlers_admin.go` | — |
| изменить | `api/internal/api/handlers_admin_portal.go` | — |
| изменить | `api/internal/api/handlers_admin_users.go` | — |
| изменить | `api/internal/api/handlers_moderation.go` | — |
| изменить | `api/internal/api/money.go` | — |
| изменить | `api/internal/auth/token.go` | — |
| изменить | `api/internal/auth/token_test.go` | — |
| изменить | `api/internal/config/config_test.go` | — |
| изменить | `api/internal/email/email_test.go` | — |
| изменить | `api/internal/httpx/context.go` | лимиты текста считаются символами по всему API |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/admin_users_test.go api/internal/api/handlers_admin.go api/internal/api/handlers_admin_portal.go \
        api/internal/api/handlers_admin_users.go api/internal/api/handlers_moderation.go api/internal/api/moderation_test.go \
        api/internal/api/money.go api/internal/api/portal_test.go api/internal/api/server.go \
        api/internal/auth/token.go api/internal/auth/token_test.go api/internal/config/config_test.go \
        api/internal/email/email_test.go api/internal/httpx/context.go api/internal/store/admin.go \
        api/internal/store/moderation.go api/internal/store/reports.go
git commit -m "BF-81 Count text limits in the remaining handlers"
git push -u origin phase-10/<ваше-имя>-BF-81
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-10` ← ветка фазы, **не** `main`
- **compare:** `phase-10/<ваше-имя>-BF-81`
- **заголовок:** `BF-81 Count text limits in the remaining handlers`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Вы капитан, поэтому свой PR вливаете сами — но только когда CI зелёный. Порядок ревью и слияния — в `captain.md`.

---

## Задача 4 из 4 · пятница 20 ноября

### `BF-B5 Stop sharing notification state on the server`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-10 && git pull
git checkout -b phase-10/<ваше-имя>-BF-B5
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-10/aslan-BF-B5`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| изменить | `api/internal/api/notify.go` | — |
| изменить | `api/internal/api/server.go` | — |
| изменить | `api/internal/email/email.go` | гонка; общее поле **удалено** |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/notify.go api/internal/api/server.go api/internal/email/email.go
git commit -m "BF-B5 Stop sharing notification state on the server"
git push -u origin phase-10/<ваше-имя>-BF-B5
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-10` ← ветка фазы, **не** `main`
- **compare:** `phase-10/<ваше-имя>-BF-B5`
- **заголовок:** `BF-B5 Stop sharing notification state on the server`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Вы капитан, поэтому свой PR вливаете сами — но только когда CI зелёный. Порядок ревью и слияния — в `captain.md`.

---

## суббота 21 ноября — ревью

Своего кода больше не пишем. Вы капитан: ваша работа в этот день — прочитать и влить все оставшиеся PR. Порядок — в `captain.md`.

## воскресенье 22 ноября — закрытие фазы

Приходите на разбор. Убедитесь, что ваши задачи в `Done` — их переводит капитан после слияния. Сразу после разбора заводите свои задачи следующей фазы.

---

## Кто смотрит ваш PR

Все PR смотрит и вливает **капитан** — то есть вы. Что проверять и как вливать, расписано в `captain.md` этой же папки.

---

## Если что-то пошло не так

| Ситуация | Что делать |
| --- | --- |
| CI покраснел | Чините у себя, делаете ещё коммит в ту же ветку, пушите. PR обновится сам. |
| Капитан вернул с замечаниями | Правите, коммитите в ту же ветку, пишете в PR «готово». Новый PR не нужен. |
| Забыли файл в коммите | Добавляете вторым коммитом в ту же ветку. Правило «одна задача — один коммит» про то, что приходит один смысловой шаг, а не про запрет себя исправлять. |
| Не успеваете к концу фазы | Говорите на разборе. Задача переезжает в следующую фазу **со своим номером**, новую взамен не заводят. |
| Нужен чужой файл | Пишете владельцу. Сами не правите никогда. |

