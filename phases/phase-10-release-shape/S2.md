# S2 · Ф10 — Нынешняя версия

**понедельник 16 ноября — воскресенье 22 ноября**

Ваша зона: бэкенд коммерции: события, билеты, checkout, возвраты.

| | |
| --- | --- |
| Задач у вас в этой фазе | **4** |
| Значит коммитов | **4** — по одному на задачу |
| Ваш PR смотрит и вливает | **S1 — капитан** |

---

## Шаг 0 · воскресенье 15 ноября — завести свои задачи в Jira

На разборе фазы. **До того, как написана первая строчка кода.**

Создайте 4 задачи в колонке `To Do`. Название копируйте дословно:

- **`BF-82 Count text limits in commerce handlers`** · тип `Story` · эпик `BF-E1` · исполнитель — вы
- **`BF-83 Cut Cyrillic text by characters on the ticket`** · тип `Story` · эпик `BF-E5` · исполнитель — вы
- **`BF-B7 Give a suspended event its own code`** · тип `Bug` · — · исполнитель — вы
- **`BF-B8 Name the refund conflict`** · тип `Bug` · — · исполнитель — вы

Пока задачи не заведены у всех пятерых, фаза не начинается.

---

## Задача 1 из 4 · понедельник 16 ноября

### `BF-82 Count text limits in commerce handlers`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-10 && git pull
git checkout -b phase-10/<ваше-имя>-BF-82
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-10/aslan-BF-82`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| изменить | `api/internal/api/handlers_events.go` | — |
| изменить | `api/internal/api/handlers_ticket_types.go` | — |
| изменить | `api/internal/api/handlers_checkout.go` | — |
| изменить | `api/internal/api/handlers_uploads.go` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/handlers_checkout.go api/internal/api/handlers_events.go api/internal/api/handlers_ticket_types.go \
        api/internal/api/handlers_uploads.go
git commit -m "BF-82 Count text limits in commerce handlers"
git push -u origin phase-10/<ваше-имя>-BF-82
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-10` ← ветка фазы, **не** `main`
- **compare:** `phase-10/<ваше-имя>-BF-82`
- **заголовок:** `BF-82 Count text limits in commerce handlers`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 2 из 4 · вторник 17 ноября

### `BF-83 Cut Cyrillic text by characters on the ticket`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-10 && git pull
git checkout -b phase-10/<ваше-имя>-BF-83
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-10/aslan-BF-83`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `api/internal/store/my_orders.go` | — |
| создать | `api/internal/api/handlers_my_orders.go` | — |
| создать | `api/internal/api/my_orders_test.go` | — |
| изменить | `api/internal/api/campaigns_test.go` | — |
| изменить | `api/internal/api/cancellations_test.go` | — |
| изменить | `api/internal/api/holds_test.go` | то же для коммерческих хендлеров |
| изменить | `api/internal/store/cancellations.go` | — |
| изменить | `api/internal/store/duplicate.go` | общий `conflict` дочищен до именованных кодов |
| изменить | `api/internal/calendar/ics.go` | — |
| изменить | `api/internal/calendar/ics_test.go` | — |
| изменить | `api/internal/ticketpdf/extract_test.go` | — |
| изменить | `api/internal/ticketpdf/fonts.go` | — |
| изменить | `api/internal/ticketpdf/fonts/LICENSE-fpdf.txt` | — |
| изменить | `api/internal/ticketpdf/qr.go` | — |
| изменить | `api/internal/ticketpdf/text.go` | — |
| изменить | `api/internal/ticketpdf/ticketpdf_test.go` | байтовая обрезка резала двухбайтовую букву пополам |
| изменить | `api/internal/api/handlers_cancellations.go` | — |
| изменить | `api/internal/api/handlers_promo.go` | лимиты текста считаются символами по всему API |
| изменить | `api/README.md` | зритель ищет свои заказы |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/README.md api/internal/api/campaigns_test.go api/internal/api/cancellations_test.go \
        api/internal/api/handlers_cancellations.go api/internal/api/handlers_my_orders.go api/internal/api/handlers_promo.go \
        api/internal/api/holds_test.go api/internal/api/my_orders_test.go api/internal/calendar/ics.go \
        api/internal/calendar/ics_test.go api/internal/store/cancellations.go api/internal/store/duplicate.go \
        api/internal/store/my_orders.go api/internal/ticketpdf/extract_test.go api/internal/ticketpdf/fonts.go \
        api/internal/ticketpdf/fonts/LICENSE-fpdf.txt api/internal/ticketpdf/qr.go api/internal/ticketpdf/text.go \
        api/internal/ticketpdf/ticketpdf_test.go
git commit -m "BF-83 Cut Cyrillic text by characters on the ticket"
git push -u origin phase-10/<ваше-имя>-BF-83
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-10` ← ветка фазы, **не** `main`
- **compare:** `phase-10/<ваше-имя>-BF-83`
- **заголовок:** `BF-83 Cut Cyrillic text by characters on the ticket`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 3 из 4 · среда 18 ноября

### `BF-B7 Give a suspended event its own code`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-10 && git pull
git checkout -b phase-10/<ваше-имя>-BF-B7
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-10/aslan-BF-B7`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| изменить | `api/internal/api/handlers_activation.go` | — |
| изменить | `api/internal/api/handlers_checkout.go` | — |
| изменить | `api/internal/api/activation_test.go` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/activation_test.go api/internal/api/handlers_activation.go api/internal/api/handlers_checkout.go
git commit -m "BF-B7 Give a suspended event its own code"
git push -u origin phase-10/<ваше-имя>-BF-B7
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-10` ← ветка фазы, **не** `main`
- **compare:** `phase-10/<ваше-имя>-BF-B7`
- **заголовок:** `BF-B7 Give a suspended event its own code`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 4 из 4 · четверг 19 ноября

### `BF-B8 Name the refund conflict`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-10 && git pull
git checkout -b phase-10/<ваше-имя>-BF-B8
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-10/aslan-BF-B8`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| изменить | `api/internal/api/handlers_refunds.go` | — |
| изменить | `api/internal/api/refunds_test.go` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/handlers_refunds.go api/internal/api/refunds_test.go
git commit -m "BF-B8 Name the refund conflict"
git push -u origin phase-10/<ваше-имя>-BF-B8
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-10` ← ветка фазы, **не** `main`
- **compare:** `phase-10/<ваше-имя>-BF-B8`
- **заголовок:** `BF-B8 Name the refund conflict`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## суббота 21 ноября — ревью

Своего кода больше не пишем. Если капитан вернул PR с замечаниями — правите и пишете в PR «готово». Если замечаний нет — свободны.

## воскресенье 22 ноября — закрытие фазы

Приходите на разбор. Убедитесь, что ваши задачи в `Done` — их переводит капитан после слияния. Сразу после разбора заводите свои задачи следующей фазы.

---

## Кто смотрит ваш PR

Все PR в команде смотрит и вливает **капитан (S1)**. Он проверяет, что файлы только из вашей зоны, что коммит один и назван как задача, и что CI зелёный. Он же вливает PR и переводит задачу в `Done`.

Друг друга не ревьюим: одно место принятия решений — меньше путаницы и не бывает «а я думал, ты посмотришь».

---

## Если что-то пошло не так

| Ситуация | Что делать |
| --- | --- |
| CI покраснел | Чините у себя, делаете ещё коммит в ту же ветку, пушите. PR обновится сам. |
| Капитан вернул с замечаниями | Правите, коммитите в ту же ветку, пишете в PR «готово». Новый PR не нужен. |
| Забыли файл в коммите | Добавляете вторым коммитом в ту же ветку. Правило «одна задача — один коммит» про то, что приходит один смысловой шаг, а не про запрет себя исправлять. |
| Не успеваете к концу фазы | Говорите на разборе. Задача переезжает в следующую фазу **со своим номером**, новую взамен не заводят. |
| Нужен чужой файл | Пишете владельцу. Сами не правите никогда. |

