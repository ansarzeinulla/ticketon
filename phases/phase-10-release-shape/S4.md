# S4 · Ф10 — Нынешняя версия

**понедельник 16 ноября — воскресенье 22 ноября**

Ваша зона: веб организатора и админа, аналитика, поддержка.

| | |
| --- | --- |
| Задач у вас в этой фазе | **3** |
| Значит коммитов | **3** — по одному на задачу |
| Ваш PR смотрит и вливает | **S1 — капитан** |

---

## Шаг 0 · воскресенье 15 ноября — завести свои задачи в Jira

На разборе фазы. **До того, как написана первая строчка кода.**

Создайте 3 задачи в колонке `To Do`. Название копируйте дословно:

- **`BF-85 Show reserved stock`** · тип `Story` · эпик `BF-E9` · исполнитель — вы
- **`BF-86 Localise the remaining dashboard screens`** · тип `Story` · эпик `BF-E11` · исполнитель — вы
- **`BF-B9 Name the reversal conflict`** · тип `Bug` · — · исполнитель — вы

Пока задачи не заведены у всех пятерых, фаза не начинается.

---

## Задача 1 из 3 · понедельник 16 ноября

### `BF-85 Show reserved stock`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-10 && git pull
git checkout -b phase-10/<ваше-имя>-BF-85
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-10/aslan-BF-85`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| переименовать | `web/src/components/stats-panel.tsx` → `web/src/components/event-analytics.tsx` | панель выросла в аналитику события |
| изменить | `api/internal/store/analytics.go` | — |
| создать | `api/internal/api/payout_figures_test.go` | — |
| изменить | `web/src/components/event-analytics.tsx` | — |
| создать | `api/internal/api/reserved_counts_test.go` | — |
| удалить | `web/src/app/(app)/dashboard/analytics/page.tsx` | страница вложена в карточку события |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git mv web/src/components/stats-panel.tsx web/src/components/event-analytics.tsx
git add -A api/internal/api/payout_figures_test.go api/internal/api/reserved_counts_test.go api/internal/store/analytics.go \
        web/src/app/(app)/dashboard/analytics/page.tsx web/src/components/event-analytics.tsx
git commit -m "BF-85 Show reserved stock"
git push -u origin phase-10/<ваше-имя>-BF-85
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-10` ← ветка фазы, **не** `main`
- **compare:** `phase-10/<ваше-имя>-BF-85`
- **заголовок:** `BF-85 Show reserved stock`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 2 из 3 · вторник 17 ноября

### `BF-86 Localise the remaining dashboard screens`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-10 && git pull
git checkout -b phase-10/<ваше-имя>-BF-86
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-10/aslan-BF-86`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `web/src/app/(app)/orders/page.tsx` | — |
| изменить | `web/src/components/site-header.tsx` | — |
| изменить | `api/internal/store/support.go` | общий `conflict` дочищен до именованных кодов |
| изменить | `web/src/components/campaign-manager.tsx` | — |
| изменить | `web/src/components/event-analytics-tracker.tsx` | — |
| изменить | `web/src/components/event-timeline.tsx` | — |
| изменить | `web/src/lib/analytics-config.ts` | последние строки уехали в словарь |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/store/support.go web/src/app/(app)/orders/page.tsx web/src/components/campaign-manager.tsx \
        web/src/components/event-analytics-tracker.tsx web/src/components/event-timeline.tsx web/src/components/site-header.tsx \
        web/src/lib/analytics-config.ts
git commit -m "BF-86 Localise the remaining dashboard screens"
git push -u origin phase-10/<ваше-имя>-BF-86
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-10` ← ветка фазы, **не** `main`
- **compare:** `phase-10/<ваше-имя>-BF-86`
- **заголовок:** `BF-86 Localise the remaining dashboard screens`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 3 из 3 · среда 18 ноября

### `BF-B9 Name the reversal conflict`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-10 && git pull
git checkout -b phase-10/<ваше-имя>-BF-B9
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-10/aslan-BF-B9`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| изменить | `api/internal/api/handlers_support.go` | — |
| изменить | `api/internal/api/support_test.go` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/handlers_support.go api/internal/api/support_test.go
git commit -m "BF-B9 Name the reversal conflict"
git push -u origin phase-10/<ваше-имя>-BF-B9
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-10` ← ветка фазы, **не** `main`
- **compare:** `phase-10/<ваше-имя>-BF-B9`
- **заголовок:** `BF-B9 Name the reversal conflict`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## суббота 21 ноября — ревью

Своего кода больше не пишем. Если капитан вернул PR с замечаниями — правите и пишете в PR «готово». Если замечаний нет — свободны.

## воскресенье 22 ноября — закрытие фазы

Приходите на разбор. Убедитесь, что ваши задачи в `Done` — их переводит капитан после слияния. Сразу после разбора заводите свои задачи следующей фазы.

---

## Кто смотрит ваш PR

Все PR в команде смотрит и вливает **капитан (S1)**. Он проверяет, что файлы только из вашей зоны, что коммит один и назван как задача, и что CI зелёный. Он же вливает PR и переводит задачу в `Done`.

Друг друга не ревьюим: одно место принятия решений — меньше путаницы и не бывает «а я думал, ты посмотришь».

---

## Если что-то пошло не так

| Ситуация | Что делать |
| --- | --- |
| CI покраснел | Чините у себя, делаете ещё коммит в ту же ветку, пушите. PR обновится сам. |
| Капитан вернул с замечаниями | Правите, коммитите в ту же ветку, пишете в PR «готово». Новый PR не нужен. |
| Забыли файл в коммите | Добавляете вторым коммитом в ту же ветку. Правило «одна задача — один коммит» про то, что приходит один смысловой шаг, а не про запрет себя исправлять. |
| Не успеваете к концу фазы | Говорите на разборе. Задача переезжает в следующую фазу **со своим номером**, новую взамен не заводят. |
| Нужен чужой файл | Пишете владельцу. Сами не правите никогда. |

