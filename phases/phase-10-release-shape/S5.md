# S5 · Ф10 — Нынешняя версия

**понедельник 16 ноября — воскресенье 22 ноября**

Ваша зона: мобильное приложение, QA, DevOps.

| | |
| --- | --- |
| Задач у вас в этой фазе | **3** |
| Значит коммитов | **3** — по одному на задачу |
| Ваш PR смотрит и вливает | **S1 — капитан** |

---

## Шаг 0 · воскресенье 15 ноября — завести свои задачи в Jira

На разборе фазы. **До того, как написана первая строчка кода.**

Создайте 3 задачи в колонке `To Do`. Название копируйте дословно:

- **`BF-87 Add the Phase 8, 9 and 10 specifications`** · тип `Story` · эпик `BF-E12` · исполнитель — вы
- **`BF-88 Point the scanner at the queue`** · тип `Story` · эпик `BF-E6` · исполнитель — вы
- **`BF-B10 Name the check-in reversal conflict`** · тип `Bug` · — · исполнитель — вы

Пока задачи не заведены у всех пятерых, фаза не начинается.

---

## Задача 1 из 3 · вторник 17 ноября

### `BF-87 Add the Phase 8, 9 and 10 specifications`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-10 && git pull
git checkout -b phase-10/<ваше-имя>-BF-87
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-10/aslan-BF-87`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `8.md` | — |
| создать | `9.md` | — |
| создать | `10.md` | — |
| удалить | `docs/api-notes.md` | вытеснен разделом в `api/README.md` |
| удалить | `docs/decisions.md` | вытеснен разделом в `README.md` |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверять сборку нечего** — в этом коммите только текст.

**5. Коммит и пуш:**

```bash
git add -A 10.md 8.md 9.md \
        docs/api-notes.md docs/decisions.md
git commit -m "BF-87 Add the Phase 8, 9 and 10 specifications"
git push -u origin phase-10/<ваше-имя>-BF-87
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-10` ← ветка фазы, **не** `main`
- **compare:** `phase-10/<ваше-имя>-BF-87`
- **заголовок:** `BF-87 Add the Phase 8, 9 and 10 specifications`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 2 из 3 · среда 18 ноября

### `BF-88 Point the scanner at the queue`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-10 && git pull
git checkout -b phase-10/<ваше-имя>-BF-88
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-10/aslan-BF-88`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| переименовать | `mobile/app/scanner.tsx` → `mobile/app/scan/[eventId].tsx` | сканер стал маршрутом на событие |
| создать | `api/internal/store/offline.go` | — |
| создать | `api/internal/api/handlers_offline.go` | — |
| создать | `api/internal/api/offline_test.go` | — |
| создать | `mobile/lib/offline.ts` | — |
| создать | `mobile/lib/offline-db.ts` | — |
| создать | `mobile/lib/use-connectivity.ts` | — |
| создать | `mobile/app/offline/[eventId].tsx` | — |
| изменить | `mobile/app/scan/[eventId].tsx` | — |
| изменить | `mobile/.env.example` | — |
| изменить | `mobile/.gitignore` | — |
| изменить | `mobile/AGENTS.md` | — |
| изменить | `mobile/CLAUDE.md` | — |
| изменить | `mobile/LICENSE` | — |
| изменить | `mobile/README.md` | — |
| изменить | `mobile/app.json` | — |
| изменить | `mobile/app/_layout.tsx` | — |
| изменить | `mobile/app/attendees/[eventId].tsx` | — |
| изменить | `mobile/app/events.tsx` | — |
| изменить | `mobile/app/index.tsx` | — |
| изменить | `mobile/app/login.tsx` | — |
| изменить | `mobile/components/ScanResultOverlay.tsx` | — |
| изменить | `mobile/lib/api.ts` | — |
| изменить | `mobile/lib/auth-context.tsx` | — |
| изменить | `mobile/lib/config.ts` | — |
| изменить | `mobile/lib/session.ts` | — |
| изменить | `mobile/lib/theme.ts` | — |
| изменить | `mobile/lib/types.ts` | — |
| изменить | `mobile/package-lock.json` | — |
| изменить | `mobile/package.json` | — |
| изменить | `mobile/tsconfig.json` | каждый экран теперь умеет работать без сети |
| удалить | `mobile/lib/storage.ts` | вытеснен expo-sqlite |
| изменить | `bilet.md` | формулировки лимитов разошлись с кодом |
| изменить | `1.md` | — |
| изменить | `2.md` | лимиты в символах, а не в байтах |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
make scan-check    # типы мобильного приложения
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git mv mobile/app/scanner.tsx mobile/app/scan/[eventId].tsx
git add -A 1.md 2.md api/internal/api/handlers_offline.go \
        api/internal/api/offline_test.go api/internal/store/offline.go bilet.md \
        mobile/.env.example mobile/.gitignore mobile/AGENTS.md \
        mobile/CLAUDE.md mobile/LICENSE mobile/README.md \
        mobile/app.json mobile/app/_layout.tsx mobile/app/attendees/[eventId].tsx \
        mobile/app/events.tsx mobile/app/index.tsx mobile/app/login.tsx \
        mobile/app/offline/[eventId].tsx mobile/app/scan/[eventId].tsx mobile/components/ScanResultOverlay.tsx \
        mobile/lib/api.ts mobile/lib/auth-context.tsx mobile/lib/config.ts \
        mobile/lib/offline-db.ts mobile/lib/offline.ts mobile/lib/session.ts \
        mobile/lib/storage.ts mobile/lib/theme.ts mobile/lib/types.ts \
        mobile/lib/use-connectivity.ts mobile/package-lock.json mobile/package.json \
        mobile/tsconfig.json
git commit -m "BF-88 Point the scanner at the queue"
git push -u origin phase-10/<ваше-имя>-BF-88
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-10` ← ветка фазы, **не** `main`
- **compare:** `phase-10/<ваше-имя>-BF-88`
- **заголовок:** `BF-88 Point the scanner at the queue`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 3 из 3 · четверг 19 ноября

### `BF-B10 Name the check-in reversal conflict`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-10 && git pull
git checkout -b phase-10/<ваше-имя>-BF-B10
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-10/aslan-BF-B10`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| изменить | `api/internal/api/handlers_checkin.go` | — |
| изменить | `api/internal/api/checkin_test.go` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/checkin_test.go api/internal/api/handlers_checkin.go
git commit -m "BF-B10 Name the check-in reversal conflict"
git push -u origin phase-10/<ваше-имя>-BF-B10
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-10` ← ветка фазы, **не** `main`
- **compare:** `phase-10/<ваше-имя>-BF-B10`
- **заголовок:** `BF-B10 Name the check-in reversal conflict`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## суббота 21 ноября — ревью

Своего кода больше не пишем. Если капитан вернул PR с замечаниями — правите и пишете в PR «готово». Если замечаний нет — свободны.

## воскресенье 22 ноября — закрытие фазы

Приходите на разбор. Убедитесь, что ваши задачи в `Done` — их переводит капитан после слияния. Сразу после разбора заводите свои задачи следующей фазы.

---

## Кто смотрит ваш PR

Все PR в команде смотрит и вливает **капитан (S1)**. Он проверяет, что файлы только из вашей зоны, что коммит один и назван как задача, и что CI зелёный. Он же вливает PR и переводит задачу в `Done`.

Друг друга не ревьюим: одно место принятия решений — меньше путаницы и не бывает «а я думал, ты посмотришь».

---

## Если что-то пошло не так

| Ситуация | Что делать |
| --- | --- |
| CI покраснел | Чините у себя, делаете ещё коммит в ту же ветку, пушите. PR обновится сам. |
| Капитан вернул с замечаниями | Правите, коммитите в ту же ветку, пишете в PR «готово». Новый PR не нужен. |
| Забыли файл в коммите | Добавляете вторым коммитом в ту же ветку. Правило «одна задача — один коммит» про то, что приходит один смысловой шаг, а не про запрет себя исправлять. |
| Не успеваете к концу фазы | Говорите на разборе. Задача переезжает в следующую фазу **со своим номером**, новую взамен не заводят. |
| Нужен чужой файл | Пишете владельцу. Сами не правите никогда. |

