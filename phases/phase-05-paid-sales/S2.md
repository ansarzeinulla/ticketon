# S2 · Ф5 — Платные продажи

**понедельник 12 октября — воскресенье 18 октября**

Ваша зона: бэкенд коммерции: события, билеты, checkout, возвраты.

| | |
| --- | --- |
| Задач у вас в этой фазе | **2** |
| Значит коммитов | **2** — по одному на задачу |
| Ваш PR смотрит и вливает | **S1 — капитан** |

---

## Шаг 0 · воскресенье 11 октября — завести свои задачи в Jira

На разборе фазы. **До того, как написана первая строчка кода.**

Создайте 2 задачи в колонке `To Do`. Название копируйте дословно:

- **`BF-39 Add seat inventory`** · тип `Story` · эпик `BF-E4` · исполнитель — вы
- **`BF-40 Gate paid sales behind activation`** · тип `Story` · эпик `BF-E4` · исполнитель — вы

Пока задачи не заведены у всех пятерых, фаза не начинается.

---

## Задача 1 из 2 · вторник 13 октября

### `BF-39 Add seat inventory`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-05 && git pull
git checkout -b phase-05/<ваше-имя>-BF-39
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-05/aslan-BF-39`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| изменить | `api/internal/store/checkout.go` | — |
| удалить | `api/internal/api/handlers_inventory.go` | ручка вложена в checkout |
| удалить | `api/internal/store/inventory.go` | логика вложена в `checkout.go` вместе с блокировкой строк |
| изменить | `api/internal/api/handlers_checkout.go` | — |
| изменить | `api/internal/api/checkout_test.go` | — |
| создать | `api/internal/store/seating.go` | — |
| создать | `api/internal/api/handlers_seating.go` | — |
| создать | `api/internal/api/seating_test.go` | — |
| изменить | `api/internal/api/handlers_public.go` | проверка роли добавляется в каждый хендлер организатора |
| изменить | `api/README.md` | платные продажи под чек-листом |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/README.md api/internal/api/checkout_test.go api/internal/api/handlers_checkout.go \
        api/internal/api/handlers_inventory.go api/internal/api/handlers_public.go api/internal/api/handlers_seating.go \
        api/internal/api/seating_test.go api/internal/store/checkout.go api/internal/store/inventory.go \
        api/internal/store/seating.go
git commit -m "BF-39 Add seat inventory"
git push -u origin phase-05/<ваше-имя>-BF-39
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-05` ← ветка фазы, **не** `main`
- **compare:** `phase-05/<ваше-имя>-BF-39`
- **заголовок:** `BF-39 Add seat inventory`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 2 из 2 · среда 14 октября

### `BF-40 Gate paid sales behind activation`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-05 && git pull
git checkout -b phase-05/<ваше-имя>-BF-40
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-05/aslan-BF-40`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `api/internal/store/activation.go` | — |
| создать | `api/internal/api/handlers_activation.go` | — |
| изменить | `api/internal/store/checkout.go` | — |
| изменить | `api/internal/api/handlers_checkout.go` | — |
| изменить | `api/internal/api/checkout_test.go` | — |
| создать | `api/internal/api/activation_test.go` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/activation_test.go api/internal/api/checkout_test.go api/internal/api/handlers_activation.go \
        api/internal/api/handlers_checkout.go api/internal/store/activation.go api/internal/store/checkout.go
git commit -m "BF-40 Gate paid sales behind activation"
git push -u origin phase-05/<ваше-имя>-BF-40
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-05` ← ветка фазы, **не** `main`
- **compare:** `phase-05/<ваше-имя>-BF-40`
- **заголовок:** `BF-40 Gate paid sales behind activation`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## суббота 17 октября — ревью

Своего кода больше не пишем. Если капитан вернул PR с замечаниями — правите и пишете в PR «готово». Если замечаний нет — свободны.

## воскресенье 18 октября — закрытие фазы

Приходите на разбор. Убедитесь, что ваши задачи в `Done` — их переводит капитан после слияния. Сразу после разбора заводите свои задачи следующей фазы.

---

## Кто смотрит ваш PR

Все PR в команде смотрит и вливает **капитан (S1)**. Он проверяет, что файлы только из вашей зоны, что коммит один и назван как задача, и что CI зелёный. Он же вливает PR и переводит задачу в `Done`.

Друг друга не ревьюим: одно место принятия решений — меньше путаницы и не бывает «а я думал, ты посмотришь».

---

## Если что-то пошло не так

| Ситуация | Что делать |
| --- | --- |
| CI покраснел | Чините у себя, делаете ещё коммит в ту же ветку, пушите. PR обновится сам. |
| Капитан вернул с замечаниями | Правите, коммитите в ту же ветку, пишете в PR «готово». Новый PR не нужен. |
| Забыли файл в коммите | Добавляете вторым коммитом в ту же ветку. Правило «одна задача — один коммит» про то, что приходит один смысловой шаг, а не про запрет себя исправлять. |
| Не успеваете к концу фазы | Говорите на разборе. Задача переезжает в следующую фазу **со своим номером**, новую взамен не заводят. |
| Нужен чужой файл | Пишете владельцу. Сами не правите никогда. |

