# S5 · Ф7 — Контроль входа

**понедельник 26 октября — воскресенье 1 ноября**

Ваша зона: мобильное приложение, QA, DevOps.

| | |
| --- | --- |
| Задач у вас в этой фазе | **3** |
| Значит коммитов | **3** — по одному на задачу |
| Ваш PR смотрит и вливает | **S1 — капитан** |

---

## Шаг 0 · воскресенье 25 октября — завести свои задачи в Jira

На разборе фазы. **До того, как написана первая строчка кода.**

Создайте 3 задачи в колонке `To Do`. Название копируйте дословно:

- **`BF-57 Scaffold the Expo scanner`** · тип `Story` · эпик `BF-E6` · исполнитель — вы
- **`BF-58 Scan and admit a ticket`** · тип `Story` · эпик `BF-E6` · исполнитель — вы
- **`BF-59 Document how to run the scanner`** · тип `Story` · эпик `BF-E6` · исполнитель — вы

Пока задачи не заведены у всех пятерых, фаза не начинается.

---

## Задача 1 из 3 · среда 28 октября

### `BF-57 Scaffold the Expo scanner`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-07 && git pull
git checkout -b phase-07/<ваше-имя>-BF-57
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-07/aslan-BF-57`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `mobile/package.json` | — |
| создать | `mobile/package-lock.json` | 7 124 строки, целиком |
| создать | `mobile/app.json` | — |
| создать | `mobile/assets/**` | — |
| создать | `mobile/lib/mock-tickets.ts` | сканер собран на фейковых payload'ах |
| создать | `mobile/.gitignore` | — |
| создать | `mobile/.env.example` | — |
| создать | `mobile/tsconfig.json` | — |
| создать | `mobile/README.md` | — |
| создать | `mobile/LICENSE` | — |
| создать | `mobile/AGENTS.md` | — |
| создать | `mobile/CLAUDE.md` | — |
| создать | `mobile/lib/config.ts` | — |
| создать | `mobile/lib/theme.ts` | — |
| создать | `mobile/lib/types.ts` | — |
| создать | `mobile/lib/auth-context.tsx` | — |
| создать | `mobile/app/_layout.tsx` | — |
| создать | `mobile/app/index.tsx` | — |
| создать | `mobile/app/login.tsx` | — |
| создать | `mobile/lib/api.ts` | — |
| создать | `mobile/lib/session.ts` | — |
| создать | `mobile/lib/storage.ts` | обёртка над AsyncStorage |
| создать | `mobile/app/events.tsx` | — |
| создать | `api/internal/store/staff.go` | — |
| создать | `api/internal/api/handlers_staff.go` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
make scan-check    # типы мобильного приложения
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/handlers_staff.go api/internal/store/staff.go mobile/.env.example \
        mobile/.gitignore mobile/AGENTS.md mobile/CLAUDE.md \
        mobile/LICENSE mobile/README.md mobile/app.json \
        mobile/app/_layout.tsx mobile/app/events.tsx mobile/app/index.tsx \
        mobile/app/login.tsx mobile/assets/** mobile/lib/api.ts \
        mobile/lib/auth-context.tsx mobile/lib/config.ts mobile/lib/mock-tickets.ts \
        mobile/lib/session.ts mobile/lib/storage.ts mobile/lib/theme.ts \
        mobile/lib/types.ts mobile/package-lock.json mobile/package.json \
        mobile/tsconfig.json
git commit -m "BF-57 Scaffold the Expo scanner"
git push -u origin phase-07/<ваше-имя>-BF-57
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-07` ← ветка фазы, **не** `main`
- **compare:** `phase-07/<ваше-имя>-BF-57`
- **заголовок:** `BF-57 Scaffold the Expo scanner`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 2 из 3 · четверг 29 октября

### `BF-58 Scan and admit a ticket`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-07 && git pull
git checkout -b phase-07/<ваше-имя>-BF-58
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-07/aslan-BF-58`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `mobile/app/scanner.tsx` | — |
| создать | `api/internal/store/checkin.go` | — |
| создать | `api/internal/api/handlers_checkin.go` | — |
| удалить | `mobile/lib/mock-tickets.ts` | вытеснен реальным ответом гейта |
| создать | `mobile/components/ScanResultOverlay.tsx` | — |
| создать | `api/internal/api/checkin_test.go` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
make scan-check    # типы мобильного приложения
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/checkin_test.go api/internal/api/handlers_checkin.go api/internal/store/checkin.go \
        mobile/app/scanner.tsx mobile/components/ScanResultOverlay.tsx mobile/lib/mock-tickets.ts
git commit -m "BF-58 Scan and admit a ticket"
git push -u origin phase-07/<ваше-имя>-BF-58
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-07` ← ветка фазы, **не** `main`
- **compare:** `phase-07/<ваше-имя>-BF-58`
- **заголовок:** `BF-58 Scan and admit a ticket`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 3 из 3 · пятница 30 октября

### `BF-59 Document how to run the scanner`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-07 && git pull
git checkout -b phase-07/<ваше-имя>-BF-59
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-07/aslan-BF-59`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `mobile/app/attendees/[eventId].tsx` | — |
| изменить | `api/internal/api/handlers_checkin.go` | — |
| создать | `5.md` | — |
| изменить | `api/scripts/smoke_test.sh` | — |
| изменить | `.github/workflows/ci.yml` | дымовой прогон после сборки |
| изменить | `README.md` | — |
| изменить | `mobile/README.md` | — |
| изменить | `Makefile` | Expo требует отдельных шагов |
| изменить | `3.md` | — |
| изменить | `4.md` | суммы в ожиданиях сдвинулись на 3.5% |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
make scan-check    # типы мобильного приложения
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A .github/workflows/ci.yml 3.md 4.md \
        5.md Makefile README.md \
        api/internal/api/handlers_checkin.go api/scripts/smoke_test.sh mobile/README.md \
        mobile/app/attendees/[eventId].tsx
git commit -m "BF-59 Document how to run the scanner"
git push -u origin phase-07/<ваше-имя>-BF-59
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-07` ← ветка фазы, **не** `main`
- **compare:** `phase-07/<ваше-имя>-BF-59`
- **заголовок:** `BF-59 Document how to run the scanner`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## суббота 31 октября — ревью

Своего кода больше не пишем. Если капитан вернул PR с замечаниями — правите и пишете в PR «готово». Если замечаний нет — свободны.

## воскресенье 1 ноября — закрытие фазы

Приходите на разбор. Убедитесь, что ваши задачи в `Done` — их переводит капитан после слияния. Сразу после разбора заводите свои задачи следующей фазы.

---

## Кто смотрит ваш PR

Все PR в команде смотрит и вливает **капитан (S1)**. Он проверяет, что файлы только из вашей зоны, что коммит один и назван как задача, и что CI зелёный. Он же вливает PR и переводит задачу в `Done`.

Друг друга не ревьюим: одно место принятия решений — меньше путаницы и не бывает «а я думал, ты посмотришь».

---

## Если что-то пошло не так

| Ситуация | Что делать |
| --- | --- |
| CI покраснел | Чините у себя, делаете ещё коммит в ту же ветку, пушите. PR обновится сам. |
| Капитан вернул с замечаниями | Правите, коммитите в ту же ветку, пишете в PR «готово». Новый PR не нужен. |
| Забыли файл в коммите | Добавляете вторым коммитом в ту же ветку. Правило «одна задача — один коммит» про то, что приходит один смысловой шаг, а не про запрет себя исправлять. |
| Не успеваете к концу фазы | Говорите на разборе. Задача переезжает в следующую фазу **со своим номером**, новую взамен не заводят. |
| Нужен чужой файл | Пишете владельцу. Сами не правите никогда. |

