# S4 · Ф9 — Аналитика, кампании, i18n

**понедельник 9 ноября — воскресенье 15 ноября**

Ваша зона: веб организатора и админа, аналитика, поддержка.

| | |
| --- | --- |
| Задач у вас в этой фазе | **3** |
| Значит коммитов | **3** — по одному на задачу |
| Ваш PR смотрит и вливает | **S1 — капитан** |

---

## Шаг 0 · воскресенье 8 ноября — завести свои задачи в Jira

На разборе фазы. **До того, как написана первая строчка кода.**

Создайте 3 задачи в колонке `To Do`. Название копируйте дословно:

- **`BF-77 Compute analytics from operational rows`** · тип `Story` · эпик `BF-E9` · исполнитель — вы
- **`BF-78 Localise the dashboard`** · тип `Story` · эпик `BF-E11` · исполнитель — вы
- **`BF-B4 Read the GA4 id from a server-safe module`** · тип `Bug` · — · исполнитель — вы

Пока задачи не заведены у всех пятерых, фаза не начинается.

---

## Задача 1 из 3 · вторник 10 ноября

### `BF-77 Compute analytics from operational rows`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-09 && git pull
git checkout -b phase-09/<ваше-имя>-BF-77
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-09/aslan-BF-77`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `api/internal/store/analytics.go` | — |
| создать | `api/internal/api/handlers_analytics.go` | — |
| создать | `web/src/app/(app)/dashboard/analytics/page.tsx` | аналитика жила отдельной страницей |
| создать | `web/src/components/stats-panel.tsx` | — |
| создать | `api/internal/api/analytics_test.go` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/analytics_test.go api/internal/api/handlers_analytics.go api/internal/store/analytics.go \
        web/src/app/(app)/dashboard/analytics/page.tsx web/src/components/stats-panel.tsx
git commit -m "BF-77 Compute analytics from operational rows"
git push -u origin phase-09/<ваше-имя>-BF-77
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-09` ← ветка фазы, **не** `main`
- **compare:** `phase-09/<ваше-имя>-BF-77`
- **заголовок:** `BF-77 Compute analytics from operational rows`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 2 из 3 · среда 11 ноября

### `BF-78 Localise the dashboard`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-09 && git pull
git checkout -b phase-09/<ваше-имя>-BF-78
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-09/aslan-BF-78`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| изменить | `web/src/components/stats-panel.tsx` | — |
| изменить | `web/src/app/(app)/dashboard/events/[id]/page.tsx` | — |
| изменить | `api/internal/api/handlers_analytics.go` | — |
| создать | `web/src/components/event-timeline.tsx` | — |
| создать | `web/src/components/campaign-manager.tsx` | — |
| изменить | `web/src/app/(app)/dashboard/events/[id]/page.tsx` | — |
| создать | `web/src/lib/analytics.ts` | — |
| создать | `web/src/components/analytics-scripts.tsx` | — |
| создать | `web/src/components/event-analytics-tracker.tsx` | — |
| изменить | `web/src/app/(app)/admin/admin-portal.tsx` | — |
| изменить | `web/src/app/(app)/admin/page.tsx` | — |
| изменить | `web/src/app/(app)/admin/reports/page.tsx` | — |
| изменить | `web/src/app/(app)/admin/reports/reports-queue.tsx` | — |
| изменить | `web/src/app/(app)/dashboard/events/[id]/edit/edit-event-form.tsx` | — |
| изменить | `web/src/app/(app)/dashboard/events/[id]/edit/page.tsx` | — |
| изменить | `web/src/app/(app)/dashboard/events/[id]/event-detail.tsx` | — |
| изменить | `web/src/app/(app)/dashboard/page.tsx` | — |
| изменить | `web/src/app/(app)/dashboard/profile/page.tsx` | — |
| изменить | `web/src/app/(app)/dashboard/profile/profile-form.tsx` | — |
| изменить | `web/src/app/(app)/events/new/create-event-form.tsx` | — |
| изменить | `web/src/app/(app)/layout.tsx` | — |
| изменить | `web/src/app/api/auth/login/route.ts` | — |
| изменить | `web/src/app/api/auth/logout/route.ts` | — |
| изменить | `web/src/app/api/auth/me/route.ts` | — |
| изменить | `web/src/app/api/auth/register/route.ts` | — |
| изменить | `web/src/app/api/proxy/[...path]/route.ts` | — |
| изменить | `web/src/components/activation-checklist.tsx` | — |
| изменить | `web/src/components/attendee-list.tsx` | — |
| изменить | `web/src/components/event-form-fields.tsx` | — |
| изменить | `web/src/components/image-upload.tsx` | — |
| изменить | `web/src/components/moderation-action.tsx` | — |
| изменить | `web/src/components/staff-manager.tsx` | — |
| изменить | `web/src/components/support-inbox.tsx` | — |
| изменить | `web/src/components/support-thread.tsx` | — |
| изменить | `web/src/components/ticket-type-manager.tsx` | то же самое для экранов организатора |
| изменить | `web/src/lib/server-session.ts` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A api/internal/api/handlers_analytics.go web/src/app/(app)/admin/admin-portal.tsx web/src/app/(app)/admin/page.tsx \
        web/src/app/(app)/admin/reports/page.tsx web/src/app/(app)/admin/reports/reports-queue.tsx web/src/app/(app)/dashboard/events/[id]/edit/edit-event-form.tsx \
        web/src/app/(app)/dashboard/events/[id]/edit/page.tsx web/src/app/(app)/dashboard/events/[id]/event-detail.tsx web/src/app/(app)/dashboard/events/[id]/page.tsx \
        web/src/app/(app)/dashboard/page.tsx web/src/app/(app)/dashboard/profile/page.tsx web/src/app/(app)/dashboard/profile/profile-form.tsx \
        web/src/app/(app)/events/new/create-event-form.tsx web/src/app/(app)/layout.tsx web/src/app/api/auth/login/route.ts \
        web/src/app/api/auth/logout/route.ts web/src/app/api/auth/me/route.ts web/src/app/api/auth/register/route.ts \
        web/src/app/api/proxy/[...path]/route.ts web/src/components/activation-checklist.tsx web/src/components/analytics-scripts.tsx \
        web/src/components/attendee-list.tsx web/src/components/campaign-manager.tsx web/src/components/event-analytics-tracker.tsx \
        web/src/components/event-form-fields.tsx web/src/components/event-timeline.tsx web/src/components/image-upload.tsx \
        web/src/components/moderation-action.tsx web/src/components/staff-manager.tsx web/src/components/stats-panel.tsx \
        web/src/components/support-inbox.tsx web/src/components/support-thread.tsx web/src/components/ticket-type-manager.tsx \
        web/src/lib/analytics.ts web/src/lib/server-session.ts
git commit -m "BF-78 Localise the dashboard"
git push -u origin phase-09/<ваше-имя>-BF-78
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-09` ← ветка фазы, **не** `main`
- **compare:** `phase-09/<ваше-имя>-BF-78`
- **заголовок:** `BF-78 Localise the dashboard`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 3 из 3 · четверг 12 ноября

### `BF-B4 Read the GA4 id from a server-safe module`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-09 && git pull
git checkout -b phase-09/<ваше-имя>-BF-B4
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-09/aslan-BF-B4`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `web/src/lib/analytics-config.ts` | — |
| изменить | `web/src/lib/analytics.ts` | `-21/+6`, чтение id **вырезано** |
| изменить | `web/src/components/analytics-scripts.tsx` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A web/src/components/analytics-scripts.tsx web/src/lib/analytics-config.ts web/src/lib/analytics.ts
git commit -m "BF-B4 Read the GA4 id from a server-safe module"
git push -u origin phase-09/<ваше-имя>-BF-B4
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-09` ← ветка фазы, **не** `main`
- **compare:** `phase-09/<ваше-имя>-BF-B4`
- **заголовок:** `BF-B4 Read the GA4 id from a server-safe module`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## суббота 14 ноября — ревью

Своего кода больше не пишем. Если капитан вернул PR с замечаниями — правите и пишете в PR «готово». Если замечаний нет — свободны.

## воскресенье 15 ноября — закрытие фазы

Приходите на разбор. Убедитесь, что ваши задачи в `Done` — их переводит капитан после слияния. Сразу после разбора заводите свои задачи следующей фазы.

---

## Кто смотрит ваш PR

Все PR в команде смотрит и вливает **капитан (S1)**. Он проверяет, что файлы только из вашей зоны, что коммит один и назван как задача, и что CI зелёный. Он же вливает PR и переводит задачу в `Done`.

Друг друга не ревьюим: одно место принятия решений — меньше путаницы и не бывает «а я думал, ты посмотришь».

---

## Если что-то пошло не так

| Ситуация | Что делать |
| --- | --- |
| CI покраснел | Чините у себя, делаете ещё коммит в ту же ветку, пушите. PR обновится сам. |
| Капитан вернул с замечаниями | Правите, коммитите в ту же ветку, пишете в PR «готово». Новый PR не нужен. |
| Забыли файл в коммите | Добавляете вторым коммитом в ту же ветку. Правило «одна задача — один коммит» про то, что приходит один смысловой шаг, а не про запрет себя исправлять. |
| Не успеваете к концу фазы | Говорите на разборе. Задача переезжает в следующую фазу **со своим номером**, новую взамен не заводят. |
| Нужен чужой файл | Пишете владельцу. Сами не правите никогда. |

