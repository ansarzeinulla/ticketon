# S3 · Ф9 — Аналитика, кампании, i18n

**понедельник 9 ноября — воскресенье 15 ноября**

Ваша зона: веб для зрителя, типы и клиент API, локализация.

| | |
| --- | --- |
| Задач у вас в этой фазе | **3** |
| Значит коммитов | **3** — по одному на задачу |
| Ваш PR смотрит и вливает | **S1 — капитан** |

---

## Шаг 0 · воскресенье 8 ноября — завести свои задачи в Jira

На разборе фазы. **До того, как написана первая строчка кода.**

Создайте 3 задачи в колонке `To Do`. Название копируйте дословно:

- **`BF-76 Add the language negotiation dependency`** · тип `Story` · эпик `BF-E11` · исполнитель — вы
- **`BF-75 Localise the attendee pages`** · тип `Story` · эпик `BF-E11` · исполнитель — вы
- **`BF-74 Add the English dictionary`** · тип `Story` · эпик `BF-E11` · исполнитель — вы

Пока задачи не заведены у всех пятерых, фаза не начинается.

---

## Задача 1 из 3 · среда 11 ноября

### `BF-76 Add the language negotiation dependency`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-09 && git pull
git checkout -b phase-09/<ваше-имя>-BF-76
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-09/aslan-BF-76`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| изменить | `web/package.json` | — |
| изменить | `web/package-lock.json` | — |
| изменить | `web/next.config.ts` | разбор `Accept-Language` |
| изменить | `web/README.md` | kk, ru, en |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A web/README.md web/next.config.ts web/package-lock.json \
        web/package.json
git commit -m "BF-76 Add the language negotiation dependency"
git push -u origin phase-09/<ваше-имя>-BF-76
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-09` ← ветка фазы, **не** `main`
- **compare:** `phase-09/<ваше-имя>-BF-76`
- **заголовок:** `BF-76 Add the language negotiation dependency`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 2 из 3 · четверг 12 ноября

### `BF-75 Localise the attendee pages`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-09 && git pull
git checkout -b phase-09/<ваше-имя>-BF-75
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-09/aslan-BF-75`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `web/src/components/promo-box.tsx` | — |
| изменить | `web/src/lib/api.ts` | — |
| изменить | `web/src/lib/types.ts` | — |
| изменить | `web/src/app/(auth)/forgot-password/forgot-password-form.tsx` | — |
| изменить | `web/src/app/(auth)/forgot-password/page.tsx` | — |
| изменить | `web/src/app/(auth)/layout.tsx` | — |
| изменить | `web/src/app/(auth)/login/login-form.tsx` | — |
| изменить | `web/src/app/(auth)/login/page.tsx` | — |
| изменить | `web/src/app/(auth)/register/page.tsx` | — |
| изменить | `web/src/app/(auth)/register/register-form.tsx` | — |
| изменить | `web/src/app/(auth)/reset-password/page.tsx` | — |
| изменить | `web/src/app/(auth)/reset-password/reset-password-form.tsx` | — |
| изменить | `web/src/app/(auth)/verify-email/page.tsx` | — |
| изменить | `web/src/app/(auth)/verify-email/verify-email-view.tsx` | — |
| изменить | `web/src/app/error.tsx` | — |
| изменить | `web/src/app/events/[slug]/ticket-selector.tsx` | — |
| изменить | `web/src/app/events/layout.tsx` | — |
| изменить | `web/src/app/events/page.tsx` | — |
| изменить | `web/src/app/favicon.ico` | — |
| изменить | `web/src/app/globals.css` | — |
| изменить | `web/src/app/not-found.tsx` | — |
| изменить | `web/src/app/page.tsx` | — |
| изменить | `web/src/components/event-card.tsx` | — |
| изменить | `web/src/components/order-support.tsx` | — |
| изменить | `web/src/components/seat-checkout.tsx` | — |
| изменить | `web/src/components/seat-map.tsx` | — |
| изменить | `web/src/components/seated-purchase.tsx` | — |
| изменить | `web/src/components/ui/alert.tsx` | — |
| изменить | `web/src/components/ui/button.tsx` | — |
| изменить | `web/src/components/ui/field.tsx` | — |
| изменить | `web/src/components/ui/status-badge.tsx` | — |
| изменить | `web/src/lib/datetime.test.ts` | — |
| изменить | `web/src/lib/datetime.ts` | — |
| изменить | `web/src/lib/money.test.ts` | — |
| изменить | `web/src/lib/money.ts` | — |
| изменить | `web/src/lib/support.ts` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A web/src/app/(auth)/forgot-password/forgot-password-form.tsx web/src/app/(auth)/forgot-password/page.tsx web/src/app/(auth)/layout.tsx \
        web/src/app/(auth)/login/login-form.tsx web/src/app/(auth)/login/page.tsx web/src/app/(auth)/register/page.tsx \
        web/src/app/(auth)/register/register-form.tsx web/src/app/(auth)/reset-password/page.tsx web/src/app/(auth)/reset-password/reset-password-form.tsx \
        web/src/app/(auth)/verify-email/page.tsx web/src/app/(auth)/verify-email/verify-email-view.tsx web/src/app/error.tsx \
        web/src/app/events/[slug]/ticket-selector.tsx web/src/app/events/layout.tsx web/src/app/events/page.tsx \
        web/src/app/favicon.ico web/src/app/globals.css web/src/app/not-found.tsx \
        web/src/app/page.tsx web/src/components/event-card.tsx web/src/components/order-support.tsx \
        web/src/components/promo-box.tsx web/src/components/seat-checkout.tsx web/src/components/seat-map.tsx \
        web/src/components/seated-purchase.tsx web/src/components/ui/alert.tsx web/src/components/ui/button.tsx \
        web/src/components/ui/field.tsx web/src/components/ui/status-badge.tsx web/src/lib/api.ts \
        web/src/lib/datetime.test.ts web/src/lib/datetime.ts web/src/lib/money.test.ts \
        web/src/lib/money.ts web/src/lib/support.ts web/src/lib/types.ts
git commit -m "BF-75 Localise the attendee pages"
git push -u origin phase-09/<ваше-имя>-BF-75
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-09` ← ветка фазы, **не** `main`
- **compare:** `phase-09/<ваше-имя>-BF-75`
- **заголовок:** `BF-75 Localise the attendee pages`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## Задача 3 из 3 · пятница 13 ноября

### `BF-74 Add the English dictionary`

**1. Jira.** Перетащите задачу из `To Do` в `In Progress`. Сделайте это утром того дня, когда начинаете, — не раньше.

**2. Создайте ветку:**

```bash
git checkout phase-09 && git pull
git checkout -b phase-09/<ваше-имя>-BF-74
```

Замените `<ваше-имя>` на своё: ветка должна называться, например, `phase-09/aslan-BF-74`.

**3. Что сделать с файлами:**

| | Файл | Примечание |
| --- | --- | --- |
| создать | `web/src/lib/i18n/config.ts` | **только en** |
| создать | `web/src/lib/i18n/context.tsx` | **только en** |
| создать | `web/src/lib/i18n/dictionaries.ts` | **только en** |
| создать | `web/src/lib/i18n/translate.ts` | **только en** |
| создать | `web/src/lib/i18n/server.ts` | **только en** |
| изменить | `web/src/app/layout.tsx` | — |
| изменить | `web/src/lib/i18n/dictionaries.ts` | +`ru` |
| изменить | `web/src/lib/i18n/config.ts` | — |
| создать | `web/src/components/language-switcher.tsx` | — |
| создать | `web/src/app/api/locale/route.ts` | — |

Трогать можно **только эти файлы**. Если кажется, что нужен чужой — напишите его владельцу, не правьте сами.

**4. Проверьте, что собирается:**

```bash
make api-check     # формат, vet и тесты Go
make web-check     # линт, типы и тесты фронта
```

Красный результат — не коммитим, чиним.

**5. Коммит и пуш:**

```bash
git add -A web/src/app/api/locale/route.ts web/src/app/layout.tsx web/src/components/language-switcher.tsx \
        web/src/lib/i18n/config.ts web/src/lib/i18n/context.tsx web/src/lib/i18n/dictionaries.ts \
        web/src/lib/i18n/server.ts web/src/lib/i18n/translate.ts
git commit -m "BF-74 Add the English dictionary"
git push -u origin phase-09/<ваше-имя>-BF-74
```

Сообщение коммита — **дословно название задачи**. Не «похожее», а то же самое: по нему Jira сама свяжет коммит с задачей.

**6. Откройте Pull Request.** Зайдите на GitHub — там будет жёлтая плашка **«Compare & pull request»**. Нажмите и проверьте:

- **base:** `phase-09` ← ветка фазы, **не** `main`
- **compare:** `phase-09/<ваше-имя>-BF-74`
- **заголовок:** `BF-74 Add the English dictionary`
- **Reviewers** справа → **S1 (капитан)**

Нажмите **Create pull request**.

**7. Jira.** Перетащите задачу в `In Review` и ждите.

Дальше PR смотрит капитан. Он же его вливает и переводит задачу в `Done` — сами не вливайте и не закрывайте.

---

## суббота 14 ноября — ревью

Своего кода больше не пишем. Если капитан вернул PR с замечаниями — правите и пишете в PR «готово». Если замечаний нет — свободны.

## воскресенье 15 ноября — закрытие фазы

Приходите на разбор. Убедитесь, что ваши задачи в `Done` — их переводит капитан после слияния. Сразу после разбора заводите свои задачи следующей фазы.

---

## Кто смотрит ваш PR

Все PR в команде смотрит и вливает **капитан (S1)**. Он проверяет, что файлы только из вашей зоны, что коммит один и назван как задача, и что CI зелёный. Он же вливает PR и переводит задачу в `Done`.

Друг друга не ревьюим: одно место принятия решений — меньше путаницы и не бывает «а я думал, ты посмотришь».

---

## Если что-то пошло не так

| Ситуация | Что делать |
| --- | --- |
| CI покраснел | Чините у себя, делаете ещё коммит в ту же ветку, пушите. PR обновится сам. |
| Капитан вернул с замечаниями | Правите, коммитите в ту же ветку, пишете в PR «готово». Новый PR не нужен. |
| Забыли файл в коммите | Добавляете вторым коммитом в ту же ветку. Правило «одна задача — один коммит» про то, что приходит один смысловой шаг, а не про запрет себя исправлять. |
| Не успеваете к концу фазы | Говорите на разборе. Задача переезжает в следующую фазу **со своим номером**, новую взамен не заводят. |
| Нужен чужой файл | Пишете владельцу. Сами не правите никогда. |

