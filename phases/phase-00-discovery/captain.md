# Капитан · Ф0 — Discovery

**пятница 11 сентября — воскресенье 13 сентября** · задач в фазе: 5

Капитан — это S1. Всё, что ниже, кроме своих задач, делает он.

---

## пятница 11 сентября — открыть фазу

Это первая фаза, закрывать нечего. Сделайте разовую настройку:

**GitHub → Settings → General → Pull Requests:**

- [x] Allow merge commits
- [ ] Allow squash merging ← выключить
- [ ] Allow rebase merging ← выключить
- [x] **Automatically delete head branches** ← ветки задач будут удаляться сами

**GitHub → Settings → Branches → Add branch protection rule** для `main` и для `phase-*`:

- [x] Require a pull request before merging
- [x] Require approvals: **1** ← апрув даёте только вы
- [x] Require status checks to pass → job из `ci.yml`
- [ ] **Do not allow bypassing the above settings** ← оставить **выключенной**

Последний пункт важен. Апрувить свой PR GitHub не даёт никому, включая вас. Если запретить обход, вы не сможете влить собственную работу — апрувить её будет некому. С выключенной галочкой вы как администратор вливаете свои PR без апрува, а остальные четверо — только через ваш апрув.

**Jira → Apps → GitHub for Jira → подключить репозиторий.** После этого коммит `BF-42 …` сам прицепится к задаче `BF-42`.

**Создайте 12 эпиков:**

| Эпик | Название |
| --- | --- |
| `BF-E1` | Основание, база данных и CI |
| `BF-E2` | Идентичность, аккаунты и права |
| `BF-E3` | События и типы билетов |
| `BF-E4` | Checkout, инвентарь и оплата |
| `BF-E5` | Электронные билеты и доставка |
| `BF-E6` | Контроль входа и сканер |
| `BF-E7` | Заказы, отмены и возвраты |
| `BF-E8` | Поддержка |
| `BF-E9` | Аналитика и аудит |
| `BF-E10` | Кампании, промокоды и админ-портал |
| `BF-E11` | Публичный сайт и локализация |
| `BF-E12` | Проверка, спецификации и релиз |

**Создайте ветку фазы:**

```bash
git checkout -b phase-00
git push -u origin phase-00
```

**5. Проверьте, что заведены все 5 задач заведены.** Пока не все — фаза не начинается.

| Задача | Кто | Название |
| --- | --- | --- |
| `BF-1` | S1 | Record the delivery plan |
| `BF-2` | S2 | Sketch the API contract |
| `BF-3` | S3 | Sketch the web routes |
| `BF-4` | S5 | Add the SRS and repository skeleton |
| `BF-5` | S5 | Describe the product and the stack |

---

## Рабочие дни — смотреть PR

**Все PR в команде смотрите и вливаете вы.** Больше никто. Поэтому открытый PR не должен висеть дольше суток — иначе встанет вся фаза.

### Как смотреть один PR

Открываете PR, вкладку **«Files changed»**, и проверяете четыре вещи:

1. **Файлы только из зоны автора.** Сверяйтесь с `plan/ownership.map`. Тронул чужой файл — возвращаете, это причина отклонить сразу.
2. **Коммит один**, и его сообщение дословно совпадает с названием задачи.
3. **CI зелёный** — галочка внизу PR.
4. Код делает то, что написано в названии, и не больше.

Дальше **«Review changes»** справа сверху → **Approve** (или **Request changes** с замечаниями) → **Submit review**.

Если одобрили — сразу вливаете: **«Merge pull request»** → **«Confirm merge»**. Ветку GitHub удалит сам. После слияния **переведите задачу автора в `Done`** — автор этого не делает.

### Свои собственные PR

Свой PR вы апрувить не сможете — GitHub не даёт апрувить себя. Поэтому в настройках ветки **не включена** галочка *Do not allow bypassing the above settings*: как администратор репозитория вы вливаете свой PR без апрува, но **только когда CI зелёный**. Остальным эта лазейка недоступна.

### Каждый день пять минут

- влить всё, что пришло за сутки;
- посмотреть, нет ли карточки в `In Progress` без единого коммита;
- посмотреть, нет ли задачи, которая должна была уйти вчера, но не ушла.

Кто что должен сдать по дням:

| День | Кто | Задача |
| --- | --- | --- |
| пятница 11 сентября | S2 | `BF-2` Sketch the API contract |
| пятница 11 сентября | S5 | `BF-4` Add the SRS and repository skeleton |
| суббота 12 сентября | S1 | `BF-1` Record the delivery plan |
| суббота 12 сентября | S3 | `BF-3` Sketch the web routes |
| суббота 12 сентября | S5 | `BF-5` Describe the product and the stack |
---

## воскресенье 13 сентября — закрыть фазу

**1. Соберите ветку фазы:**

```bash
git checkout phase-00 && git pull
make api-check && make web-check
```

**2. PR `phase-00` → `main`.** Апрувит любой другой; вливаете вы.

**3. Тег:**

```bash
git checkout main && git pull
git tag phase-00-done && git push origin phase-00-done
```

**4. Тут же откройте Ф1.** Каждый заводит свои задачи: `BF-10`…`BF-9`. Инструкции — в `phases/phase-01-skeleton/`.

---

## Чего капитан делать не должен

- **Вливать свой PR с красным CI.** Права позволяют, правило — нет. Это единственное место, где вас никто не остановит, кроме вас самих.
- **Заводить задачи за других.** Каждый заводит свои — иначе непонятно, кто что понял.
- **Пушить в `main`.** Никогда и ни при каких обстоятельствах.
- **Чинить чужой код в своей ветке.** Пишете владельцу.

